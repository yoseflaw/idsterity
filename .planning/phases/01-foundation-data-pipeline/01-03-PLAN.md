---
phase: 01-foundation-data-pipeline
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/scripts/prepare-data.py
  - dashboard/scripts/word-cloud.py
  - dashboard/public/data/constants.json
  - dashboard/public/data/lembaga-totals.json
  - dashboard/public/data/summary-stats.json
  - dashboard/public/data/wordcloud-all.json
  - dashboard/public/data/wordcloud-central.json
  - dashboard/public/data/wordcloud-district.json
  - dashboard/public/data/wordcloud-lembaga.json
  - dashboard/package.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
must_haves:
  truths:
    - "constants.json exists at dashboard/public/data/ with APBN Q1 2026 deficit + historical series (Oct 2024, Full-year 2025) and BPS GDP konsumsi pemerintah Q1 2025–Q1 2026"
    - "Running uv run dashboard/scripts/prepare-data.py exits 0 and regenerates lembaga-totals.json and summary-stats.json"
    - "lembaga-totals.json entries include S4 aggregate keys: jenisCounts, metodeCounts, paguByMonth"
    - "Running uv run dashboard/scripts/word-cloud.py exits 0 and writes 4 files: wordcloud-all.json, wordcloud-central.json, wordcloud-district.json, wordcloud-lembaga.json"
    - "Each flat wordcloud file is a top-20 array of {word, count}; wordcloud-lembaga.json is a single keyed object per D-08"
    - "npm run prepare-data script in package.json runs both prepare-data.py and word-cloud.py via uv"
  artifacts:
    - path: "dashboard/public/data/constants.json"
      provides: "Hardcoded APBN deficit + GDP series for Phase 2 narrative"
      contains: "apbn"
    - path: "dashboard/scripts/prepare-data.py"
      provides: "Extended ETL producing top-30 institutions with flag counts + S4 aggregates"
      contains: "jenisPengadaan"
    - path: "dashboard/scripts/word-cloud.py"
      provides: "Per-filter word+frequency JSON for Phase 3 word cloud"
      contains: "wordcloud-lembaga.json"
    - path: "dashboard/public/data/wordcloud-all.json"
      provides: "Top-20 words across all high-inappropriate paket names"
    - path: "dashboard/public/data/wordcloud-lembaga.json"
      provides: "Keyed object of top-20 words per lembaga"
  key_links:
    - from: "dashboard/scripts/word-cloud.py"
      to: "nlp-id package"
      via: "lemmatizer import"
      pattern: "from nlp_id"
    - from: "dashboard/scripts/word-cloud.py"
      to: "inaproc-ds/outputs/*_priority.json"
      via: "JSON file iteration"
      pattern: "_priority\\.json"
    - from: "dashboard/scripts/prepare-data.py"
      to: "inaproc-ds/outputs/*.jsonl"
      via: "JSONL line iteration"
      pattern: "\\.jsonl"
---

## Phase Goal

**As a** visitor of idsterity, **I want to** see real APBN/GDP figures and procurement aggregates baked into the page, **so that** Phase 2 narrative sections (S1–S6) and Phase 3 word cloud (S8) can render immediately without runtime computation.

<objective>
Produce all five categories of static data artifacts the rest of the project depends on: (1) hand-authored `constants.json` with APBN deficit + BPS GDP figures, (2) extended `prepare-data.py` adding S4 aggregates (jenisPengadaan, metode, paguByMonth) per institution, (3) new `word-cloud.py` script that tokenizes high-inappropriate paket names through `nlp-id` and writes four word-frequency JSON files, (4) regenerated `lembaga-totals.json` and `summary-stats.json` outputs reflecting the S4 schema additions, and (5) an `npm run prepare-data` script that runs both Python scripts via `uv`.

Purpose: Without these files, Phase 2 and Phase 3 cannot start. Plan 01 (skeleton) also fetches `constants.json` at mount, so this plan is the foundation that unblocks Wave 2.

Output: 2 Python scripts (1 extended, 1 new), 1 hand-authored JSON file, 4 generated word-cloud JSON files, 2 regenerated aggregate JSON files, and 1 npm script entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-PATTERNS.md
@dashboard/scripts/prepare-data.py
@CLAUDE.md

<interfaces>
<!-- Existing record schema in inaproc-ds/outputs/*.jsonl (from CLAUDE.md): -->
<!-- Fields read by this plan: -->
<!--   r["id"]: int            -- LPSE package ID -->
<!--   r["paket"]: string      -- Procurement package name (tokenize source for word cloud) -->
<!--   r["lembaga"]: string    -- Institution name (group key) -->
<!--   r["pagu"]: int          -- Budget IDR -->
<!--   r["ownerType"]: string  -- "central" | "provinsi" | "kabkota" -->
<!--   r["jenisPengadaan"]: string  -- "Barang" | "Pekerjaan Konstruksi" | "Jasa Konsultansi" | "Jasa Lainnya" -->
<!--   r["metode"]: string     -- "Tender" | "Pengadaan Langsung" | "E-Purchasing" | ... -->
<!--   r["pemilihanDate"]: string  -- "January 2026" etc. -->
<!--   r["tags"]["isInappropriate"]: string  -- "low" | "med" | "high" -->

<!-- Existing prepare-data.py output schema (lembaga-totals.json entry): -->
<!--   { rank, name, total, count, ownerType, flaggedCount, flaggedPagu, highCount, medCount } -->
<!-- This plan APPENDS three new keys per entry: jenisCounts, metodeCounts, paguByMonth -->

<!-- New constants.json schema (Claude's discretion per CONTEXT.md "Claude's Discretion"; -->
<!-- recommended in PATTERNS.md line 542): -->
<!--   { -->
<!--     "apbn": { -->
<!--       "deficit": { -->
<!--         "oct2024": <int IDR>,    // Oct 2024 cumulative deficit -->
<!--         "fy2025":  <int IDR>,    // Full-year 2025 deficit -->
<!--         "q1_2026": <int IDR>     // Q1 2026 deficit -->
<!--       }, -->
<!--       "unit": "IDR" -->
<!--     }, -->
<!--     "gdp": { -->
<!--       "konsumsi_pemerintah": { -->
<!--         "q1_2025": <float pct contribution>, -->
<!--         "q2_2025": ..., -->
<!--         "q3_2025": ..., -->
<!--         "q4_2025": ..., -->
<!--         "q1_2026": ... -->
<!--       }, -->
<!--       "unit": "percent_contribution_yoy" -->
<!--     }, -->
<!--     "sources": [ -->
<!--       { "field": "apbn.deficit.oct2024", "url": "...", "label": "..." }, -->
<!--       ... -->
<!--     ] -->
<!--   } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author constants.json with APBN deficit + BPS GDP figures (DATA-01, DATA-02)</name>
  <files>dashboard/public/data/constants.json</files>
  <read_first>
    - .planning/REQUIREMENTS.md (DATA-01: APBN Q1 2026 deficit + Oct 2024 + Full-year 2025 series; DATA-02: BPS GDP konsumsi pemerintah Q1 2025–Q1 2026)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md (canonical schema notes)
    - .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md §constants.json (recommended schema with apbn/gdp/sources keys; camelCase per existing convention)
    - .planning/STATE.md (blocker note: DATA-01 / DATA-02 figures must be manually researched)
  </read_first>
  <action>
    Hand-author dashboard/public/data/constants.json. Research and record three APBN deficit data points (Oct 2024 cumulative deficit, Full-year 2025 deficit, Q1 2026 deficit) from Kemenkeu APBN KiTa publications and five BPS GDP konsumsi pemerintah quarterly contribution figures (Q1 2025, Q2 2025, Q3 2025, Q4 2025, Q1 2026) from BPS PDB Triwulanan press releases.

    Schema (per D-01/D-02 + PATTERNS.md §constants.json + Claude's Discretion for nesting):
    - Top-level keys: apbn, gdp, sources
    - apbn.deficit object with keys oct2024, fy2025, q1_2026 (integer IDR values; negative numbers for deficit)
    - apbn.unit: "IDR"
    - gdp.konsumsi_pemerintah object with keys q1_2025, q2_2025, q3_2025, q4_2025, q1_2026 (float percent contribution to GDP growth, year-over-year)
    - gdp.unit: "percent_contribution_yoy"
    - sources: array of { field, url, label, accessed } entries — one per data point — citing Kemenkeu APBN KiTa and BPS PDB Triwulanan publications

    Use camelCase only where it does not break the snake_case quarter keys (q1_2025 etc. stay snake_case as they are quarter identifiers, not field names). Preserve numeric types — values must be JSON numbers, not strings.

    If a Q1 2026 figure has not yet been officially published at the date of execution (2026-05-13), use the most recent provisional Kemenkeu/BPS figure available and note `"provisional": true` on that specific entry in sources. Do NOT use placeholder values like 0 or null — block on real research.
  </action>
  <verify>
    <automated>cd dashboard && python3 -c "import json; d=json.load(open('public/data/constants.json')); assert 'apbn' in d and 'gdp' in d and 'sources' in d, 'top-level keys missing'; assert set(d['apbn']['deficit'].keys()) == {'oct2024','fy2025','q1_2026'}, 'apbn.deficit keys'; assert set(d['gdp']['konsumsi_pemerintah'].keys()) == {'q1_2025','q2_2025','q3_2025','q4_2025','q1_2026'}, 'gdp keys'; assert all(isinstance(v,(int,float)) for v in d['apbn']['deficit'].values()), 'apbn values not numeric'; assert all(isinstance(v,(int,float)) for v in d['gdp']['konsumsi_pemerintah'].values()), 'gdp values not numeric'; assert isinstance(d['sources'],list) and len(d['sources']) >= 5, 'sources too short'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File `dashboard/public/data/constants.json` exists and parses as valid JSON
    - Has top-level keys `apbn`, `gdp`, `sources`
    - `apbn.deficit` has exactly three numeric entries: `oct2024`, `fy2025`, `q1_2026`
    - `apbn.unit` equals `"IDR"`
    - `gdp.konsumsi_pemerintah` has exactly five numeric entries: `q1_2025`, `q2_2025`, `q3_2025`, `q4_2025`, `q1_2026`
    - `gdp.unit` equals `"percent_contribution_yoy"`
    - `sources` is an array with at least 5 entries; each entry has `field`, `url`, `label`
    - No value in `apbn.deficit` or `gdp.konsumsi_pemerintah` is 0, null, or omitted
    - Source URLs reference Kemenkeu or BPS domains (e.g., `kemenkeu.go.id`, `bps.go.id`)
  </acceptance_criteria>
  <done>Verify command exits 0 printing "OK"; sources column lists real Kemenkeu/BPS URLs (verified by visual inspection of the JSON file).</done>
</task>

<task type="auto">
  <name>Task 2: Extend prepare-data.py with S4 aggregates and regenerate output JSON (DATA-03)</name>
  <files>dashboard/scripts/prepare-data.py, dashboard/public/data/lembaga-totals.json, dashboard/public/data/summary-stats.json</files>
  <read_first>
    - dashboard/scripts/prepare-data.py (full current file — extend, do not rewrite per PATTERNS.md line 403)
    - .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md §prepare-data.py (S4 aggregate fields jenis_counts, metode_counts, pagu_by_month — three new defaultdicts of defaultdicts)
    - .planning/REQUIREMENTS.md (DATA-03: total pagu by AI label, record counts — S4 dataset overview support)
    - CLAUDE.md §Key fields (jenisPengadaan, metode, pemilihanDate field semantics)
  </read_first>
  <action>
    Extend dashboard/scripts/prepare-data.py (do NOT rewrite — preserve existing accumulators, JSONL loop, priority loop, ranking, and output write blocks). Add three new accumulators initialized alongside the existing `totals`/`counts`/`owner_types` block:
    - `jenis_counts = collections.defaultdict(lambda: collections.defaultdict(int))` — keyed by lembaga, then by `r.get("jenisPengadaan")`
    - `metode_counts = collections.defaultdict(lambda: collections.defaultdict(int))` — keyed by lembaga, then by `r.get("metode")`
    - `pagu_by_month = collections.defaultdict(lambda: collections.defaultdict(int))` — keyed by lembaga, then by `r.get("pemilihanDate")`, value is summed `pagu`

    Update each accumulator inside the existing `for line in path.open()` block (right after the existing `total_records += 1` increment).

    Also add a top-level summary aggregate for S4 (DATA-03 explicitly calls for "total pagu by AI label, record counts" — these are dataset-wide, not per-institution):
    - `label_pagu = {"low": 0, "med": 0, "high": 0, "unflagged": 0}` and `label_counts = {...}` — populated by reading priority files for med/high and computing `unflagged = totals - flagged` for the low+unflagged residual.
    - For "low": iterate `*_priority.json` records and accumulate where `tags.isInappropriate == "low"` (these CAN appear in priority files alongside med/high — verify by reading one sample priority file before writing the loop).
    - For unflagged: `unflagged_pagu = total_pagu - sum(label_pagu.values())` and `unflagged_count = total_records - sum(label_counts.values())`.

    Update the `lembaga_totals` list comprehension to append three new keys per entry:
    - `"jenisCounts": dict(jenis_counts[name])`
    - `"metodeCounts": dict(metode_counts[name])`
    - `"paguByMonth": dict(pagu_by_month[name])`

    Update the `summary` dict to add three new keys:
    - `"labelPagu": label_pagu`
    - `"labelCounts": label_counts`
    - `"unflaggedPagu": unflagged_pagu`

    Do NOT change the existing keys (rank, name, total, count, ownerType, flaggedCount, flaggedPagu, highCount, medCount) — Plan 01 will fetch this file and depends on backward compatibility.

    After editing the script, run `uv run dashboard/scripts/prepare-data.py` to regenerate both output JSON files. If `uv` is unavailable, fall back to `python3 dashboard/scripts/prepare-data.py` (no external deps — stdlib only per PEP 723 metadata `dependencies = []`).

    The full dataset `inaproc-ds/outputs/` is ~3.2 GB and may not be present in the working tree. If `DATA_DIR` is empty, abort the run and print a clear message — do NOT commit empty output JSON files. Use `curl` per CLAUDE.md to obtain the dataset first if missing.
  </action>
  <verify>
    <automated>cd dashboard && (test -d ../inaproc-ds/outputs && ls ../inaproc-ds/outputs/*.jsonl | head -1 > /dev/null && python3 scripts/prepare-data.py && python3 -c "import json; e=json.load(open('public/data/lembaga-totals.json'))[0]; assert {'jenisCounts','metodeCounts','paguByMonth'}.issubset(e.keys()), 'S4 keys missing on entry 0'; assert isinstance(e['jenisCounts'],dict) and len(e['jenisCounts'])>=1, 'jenisCounts empty'; s=json.load(open('public/data/summary-stats.json')); assert {'labelPagu','labelCounts','unflaggedPagu'}.issubset(s.keys()), 'S4 summary keys missing'; assert set(s['labelPagu'].keys())=={'low','med','high','unflagged'}, 'labelPagu shape'; print('OK')" || echo "SKIP: inaproc-ds/outputs not present — fetch dataset first per CLAUDE.md")</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/scripts/prepare-data.py` imports remain a single line `import json, pathlib, collections` (no new imports — stdlib only)
    - Script contains three new accumulators: `jenis_counts`, `metode_counts`, `pagu_by_month` (grep test: `grep -E 'jenis_counts|metode_counts|pagu_by_month' dashboard/scripts/prepare-data.py | wc -l` returns ≥ 6, since each is referenced at init + inside the loop)
    - Script contains `label_pagu` and `label_counts` symbols (grep finds both)
    - Each entry in `dashboard/public/data/lembaga-totals.json` has keys `jenisCounts`, `metodeCounts`, `paguByMonth` (dict-typed) in addition to existing keys
    - `dashboard/public/data/summary-stats.json` has keys `labelPagu`, `labelCounts`, `unflaggedPagu` in addition to existing `totalPagu`/`totalRecords`/`flaggedCount`/`flaggedPagu`/`uniqueLembaga`
    - `labelPagu` and `labelCounts` each have exactly four keys: `low`, `med`, `high`, `unflagged`
    - Existing keys (`rank`, `name`, `total`, `count`, `ownerType`, `flaggedCount`, `flaggedPagu`, `highCount`, `medCount`) still present in `lembaga-totals.json` entries (backward compatibility)
    - Running the script exits 0 with no Python traceback (or prints SKIP message if dataset is absent)
  </acceptance_criteria>
  <done>`prepare-data.py` extended with three per-institution S4 accumulators + four-bucket dataset-wide label aggregates; regenerated `lembaga-totals.json` and `summary-stats.json` contain new keys; existing keys intact; verify command exits 0.</done>
</task>

<task type="auto">
  <name>Task 3: Create word-cloud.py producing 4 word-frequency JSON files (DATA-04) and register npm prepare-data script</name>
  <files>dashboard/scripts/word-cloud.py, dashboard/public/data/wordcloud-all.json, dashboard/public/data/wordcloud-central.json, dashboard/public/data/wordcloud-district.json, dashboard/public/data/wordcloud-lembaga.json, dashboard/package.json</files>
  <read_first>
    - dashboard/scripts/prepare-data.py (role-match analog per PATTERNS.md line 471 — copy PEP 723 header, path constants, JSONL iteration, output write/print patterns)
    - .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md §word-cloud.py (full skeleton + stopword list + per-filter output schemas D-06, D-07, D-08)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md §D-06 (word + frequency only, NO pre-computed x/y/rotation), §D-07 (4 files), §D-08 (wordcloud-lembaga.json is keyed object, NOT per-lembaga files)
    - .planning/REQUIREMENTS.md DATA-04 (nlp-id lemmatization, domain stopwords, top 20 per filter)
    - dashboard/package.json (extend `scripts` section)
  </read_first>
  <action>
    Create dashboard/scripts/word-cloud.py from scratch using prepare-data.py as the structural analog.

    PEP 723 header — copy from prepare-data.py and adapt:
    - `requires-python = ">=3.11"`
    - `dependencies = ["nlp-id"]`
    Module docstring lists the four output files and the `uv run scripts/word-cloud.py` invocation.

    Imports (single line, stdlib first): `import json, pathlib, collections, re`
    Then on a separate line: `from nlp_id.lemmatizer import Lemmatizer` (nlp-id exposes `nlp_id.lemmatizer.Lemmatizer.lemmatize(text) -> str`).

    Path constants (copy verbatim from prepare-data.py):
    - `DATA_DIR = pathlib.Path(__file__).parent.parent.parent / "inaproc-ds" / "outputs"`
    - `OUT_DIR  = pathlib.Path(__file__).parent.parent / "public" / "data"`
    - `TOP_N    = 20` (per D-06: capped at top 20 words per filter)

    Stopword set — declare as a module-level frozenset including at minimum the terms enumerated in PATTERNS.md line 530: `pengadaan`, `jasa`, `barang`, `pekerjaan`, `konstruksi`, `kegiatan`, `tahun`, `paket`, `dll`, `dan`, `yang`, `untuk`, `di`, `ke`, `dari`, `dalam`, `dengan`, `atau`, `adalah`, `pada`. Add reasonable extensions at Claude's discretion (procurement boilerplate): `tahun`, `anggaran`, `provinsi`, `kabupaten`, `kota`, `kementerian`, `belanja`, `bahan`, `alat`, plus single-letter and two-letter tokens. Final set should be ~30 entries.

    Tokenization function `tokenize(paket: str) -> list[str]`:
    1. Lowercase
    2. `re.findall(r"[a-z]+", paket)` to drop digits/punctuation
    3. Filter tokens with length < 3
    4. Filter stopwords
    5. Lemmatize each survivor via the singleton Lemmatizer
    6. Filter stopwords AGAIN after lemmatization (lemmas may collapse to a stopword)
    7. Return the resulting list

    Iterate priority files only (high-inappropriate subset per DATA-04). Source: `for path in sorted(DATA_DIR.glob("*_priority.json"))` (matches the existing pattern in prepare-data.py line 38).

    Build four counters during the single pass through priority records:
    - `counts_all: defaultdict(int)` — every record
    - `counts_central: defaultdict(int)` — where `r["ownerType"] == "central"`
    - `counts_district: defaultdict(int)` — where `r["ownerType"]` in `{"provinsi", "kabkota"}`
    - `counts_by_lembaga: defaultdict(lambda: defaultdict(int))` — keyed by lembaga name, then by word

    Filter to high-only: per DATA-04 "high-inappropriate paket names". Only process records where `r.get("tags", {}).get("isInappropriate") == "high"`. Skip med and low.

    For each high record: tokenize `r.get("paket") or ""`. For each token, increment counts_all, ownerType-specific counter, and counts_by_lembaga[lembaga].

    Output writes (4 files):
    - `wordcloud-all.json`, `wordcloud-central.json`, `wordcloud-district.json` — each a flat array of top-20 `{"word": str, "count": int}` entries, sorted by count descending. Use `sorted(counter.items(), key=lambda x: x[1], reverse=True)[:TOP_N]`.
    - `wordcloud-lembaga.json` — single keyed object `{lembaga_name: [{"word","count"}, ...]}` per D-08. For each lembaga, take its top-20 words. INCLUDE all lembaga with at least one high-inappropriate record (do not filter by lembaga size).

    Use `json.dumps(..., ensure_ascii=False, indent=2)` (matches existing pattern). Print confirmation line per file as in prepare-data.py line 81.

    No try/except. No error handling. If the dataset directory is empty, the script will naturally produce empty outputs — handle that by aborting before write if `counts_all` is empty: `if not counts_all: raise SystemExit("No priority records found — fetch dataset first")`.

    Then update `dashboard/package.json`:
    - Change `scripts.prepare-data` from `"python3 scripts/prepare-data.py"` to `"uv run scripts/prepare-data.py && uv run scripts/word-cloud.py"`. (uv is the canonical runner per PEP 723 metadata in both scripts. If a fallback is needed it can be added later — Phase 1 standardizes on uv.)
    - Do not modify other scripts entries (`dev`, `build`, `preview`).
  </action>
  <verify>
    <automated>cd dashboard && (test -d ../inaproc-ds/outputs && ls ../inaproc-ds/outputs/*_priority.json | head -1 > /dev/null && (command -v uv >/dev/null && uv run scripts/word-cloud.py || python3 -c "print('SKIP: uv not installed — install uv or run word-cloud.py with PEP 723 inline-deps tooling')") && python3 -c "import json; a=json.load(open('public/data/wordcloud-all.json')); assert isinstance(a,list) and len(a)<=20 and len(a)>=1, 'all schema'; assert all('word' in e and 'count' in e for e in a), 'flat entry shape'; c=json.load(open('public/data/wordcloud-central.json')); d=json.load(open('public/data/wordcloud-district.json')); l=json.load(open('public/data/wordcloud-lembaga.json')); assert isinstance(l,dict) and len(l)>=1, 'lembaga keyed object'; assert isinstance(next(iter(l.values())),list), 'lembaga value is list'; print('OK')" && grep -q 'word-cloud.py' package.json) || echo "SKIP: dataset or uv not present"</automated>
  </verify>
  <acceptance_criteria>
    - File `dashboard/scripts/word-cloud.py` exists with PEP 723 header containing `dependencies = ["nlp-id"]` (grep test: `grep -E 'nlp-id|nlp_id' dashboard/scripts/word-cloud.py` returns ≥ 2 matches — header + import)
    - Script imports `Lemmatizer` from `nlp_id.lemmatizer`
    - Script contains a stopword set including at minimum `pengadaan`, `jasa`, `barang`, `pekerjaan` (grep all four substrings present)
    - Script filters records by `tags.isInappropriate == "high"` (grep test: `grep -E 'high' dashboard/scripts/word-cloud.py` returns ≥ 1 match)
    - All four output files exist: `wordcloud-all.json`, `wordcloud-central.json`, `wordcloud-district.json`, `wordcloud-lembaga.json`
    - `wordcloud-all.json`, `wordcloud-central.json`, `wordcloud-district.json` are JSON arrays of length ≤ 20 (top-N cap), each entry has `word` and `count` keys
    - `wordcloud-lembaga.json` is a JSON object (not array); each value is a list of `{word, count}` entries (per D-08)
    - `dashboard/package.json` scripts.prepare-data runs both Python scripts (`grep -E 'word-cloud.py' dashboard/package.json` returns 1)
    - `dashboard/package.json` is valid JSON after edit (`python3 -c "import json; json.load(open('dashboard/package.json'))"` exits 0)
  </acceptance_criteria>
  <done>word-cloud.py exists with nlp-id integration, stopword filtering, and high-only record filter; four word-cloud JSON files generated (3 flat top-20 arrays + 1 keyed object); `npm run prepare-data` runs both scripts via uv; verify command exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| inaproc-ds → Python scripts | Untrusted external dataset (LPSE scrape) crosses into the build pipeline; malformed records could break the ETL |
| Python scripts → public/data/*.json | Generated artifacts are served as static content to all visitors; any payload here is broadcast to every reader |
| constants.json (hand-authored) → static asset | Human-authored data file becomes part of the production payload — typos or stale figures undermine the narrative |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | constants.json sources | mitigate | Each figure carries a `sources[].url` + `label` pointing to Kemenkeu/BPS publication; reviewer can verify provenance. Acceptance criteria require source URLs on `kemenkeu.go.id` or `bps.go.id` domains. |
| T-01-03-02 | Information disclosure | wordcloud-lembaga.json | accept | Output is derived from publicly accessible LPSE/SIRUP data (already public); rebroadcasting it is a transparency feature, not a leak. No PII present (no individual names — institutional aggregates only). |
| T-01-03-03 | Denial of service | prepare-data.py / word-cloud.py malformed line crash | accept | Per PATTERNS.md anti-pattern note: Python script has no error handling and a malformed JSONL line raises and aborts. Build-time only — does not affect production runtime. Phase 4 may add `try/except` if needed. |
| T-01-03-04 | Spoofing | nlp-id supply chain | mitigate | Pin to the latest stable nlp-id version when uv resolves dependencies (PEP 723 inline deps); the package is small and stdlib-adjacent. Document the version in any future RESEARCH.md note. Low value target for supply-chain attacks. |
| T-01-03-05 | Tampering | Stopword list bias | accept | Stopword choices are Claude's discretion per D-51; biased filtering could shape the word cloud narrative. Mitigation: stopword list is in source-controlled Python file, reviewable by humans before commit. |
| T-01-03-06 | Information disclosure | Large dataset accidentally committed | mitigate | `.gitignore` (existing) already excludes `inaproc-ds/outputs/`; commit only the `public/data/*.json` outputs. Verify in PR review that no JSONL/CSV from `inaproc-ds/` was staged. |
</threat_model>

<verification>
1. `dashboard/public/data/constants.json` exists, parses as valid JSON, contains real APBN + BPS figures with source URLs.
2. `dashboard/scripts/prepare-data.py` extended with S4 aggregates; backward-compatible existing keys still present in regenerated outputs.
3. `dashboard/scripts/word-cloud.py` exists, imports `nlp_id.lemmatizer`, filters high-only records, writes 4 output files matching D-06 / D-07 / D-08 schemas.
4. `dashboard/public/data/wordcloud-*.json` (4 files) exist and conform to expected shapes (flat arrays for filter-level, keyed object for lembaga-level).
5. `dashboard/package.json` scripts.prepare-data invokes both Python scripts via `uv`.
6. Running `cd dashboard && npm run prepare-data` exits 0 when dataset is present.
</verification>

<success_criteria>
- All 4 requirement IDs (DATA-01, DATA-02, DATA-03, DATA-04) have artifacts on disk
- Plan 01 (skeleton) can fetch `constants.json` without 404 in Wave 2
- Phase 3 (S8 word cloud) has all 4 word-cloud JSON files ready to consume
- Phase 2 (S4 dataset overview) has `labelPagu` + `labelCounts` + `unflaggedPagu` ready in summary-stats.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-03-SUMMARY.md`
</output>
