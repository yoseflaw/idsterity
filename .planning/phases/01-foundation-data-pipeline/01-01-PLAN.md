---
phase: 01-foundation-data-pipeline
plan: 01
type: execute
wave: 2
depends_on:
  - 01-03
files_modified:
  - dashboard/package.json
  - dashboard/package-lock.json
  - dashboard/src/App.svelte
  - dashboard/src/BarChart.svelte
  - dashboard/src/main.js
  - dashboard/src/i18n.js
  - dashboard/index.html
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04
  - FOUND-06
must_haves:
  truths:
    - "Running cd dashboard && npm install installs scrollama, @fontsource/libre-baskerville, @fontsource/source-serif-4, @fontsource/jetbrains-mono without errors"
    - "Running cd dashboard && npm run dev serves a page on localhost with no console errors and no network requests to fonts.googleapis.com or fonts.gstatic.com"
    - "Hero section renders Indonesian copy (Ke mana perginya / uang rakyat?) and a real budget figure fetched from /data/summary-stats.json"
    - "Indonesian-default i18n.js exports a `t` object with id and en sub-objects containing all 8 infrastructure keys from UI-SPEC Copywriting Contract"
    - "All CSS design tokens from UI-SPEC §CSS Token Promotion Checklist are declared on :global(:root)"
    - "POC BarChart.svelte is deleted; App.svelte does not import BarChart"
  artifacts:
    - path: "dashboard/src/App.svelte"
      provides: "Minimal hero scaffold + scrolly stub placeholder; consumes i18n.js + constants.json + summary-stats.json"
      min_lines: 80
      contains: "let lang"
    - path: "dashboard/src/i18n.js"
      provides: "Bilingual string store; flat key structure under id/en"
      contains: "export const t"
    - path: "dashboard/src/main.js"
      provides: "@fontsource imports + Svelte mount"
      contains: "@fontsource/libre-baskerville"
    - path: "dashboard/index.html"
      provides: "HTML shell without Google Fonts CDN links"
    - path: "dashboard/package.json"
      provides: "Dependency declarations including scrollama and 3 @fontsource packages"
      contains: "scrollama"
  key_links:
    - from: "dashboard/src/main.js"
      to: "@fontsource/libre-baskerville/400.css"
      via: "ES module import"
      pattern: "@fontsource/libre-baskerville"
    - from: "dashboard/src/App.svelte"
      to: "dashboard/src/i18n.js"
      via: "ES module import"
      pattern: "import .* from ['\"]\\./i18n"
    - from: "dashboard/src/App.svelte"
      to: "/data/constants.json"
      via: "fetch in onMount"
      pattern: "fetch.*constants\\.json"
    - from: "dashboard/src/App.svelte"
      to: ":global(:root) tokens"
      via: "CSS custom properties block"
      pattern: ":global\\(:root\\)"
---

## Phase Goal

**As a** visitor opening the idsterity dev server, **I want to** see a minimal hero page that renders real fetched data with self-hosted fonts and no Google CDN dependency, **so that** the skeleton is in place for Plan 02 (toggle + scrollama) and all Phase 2 narrative work begins from a clean foundation.

<objective>
Replace the POC component files with a clean Walking Skeleton: install scrollama and @fontsource dependencies, create `i18n.js`, rewrite `App.svelte` as a minimal hero + scrolly stub, prepend `@fontsource` imports to `main.js`, remove Google Fonts CDN links from `index.html`, and delete the POC `BarChart.svelte`. The skeleton fetches three JSON files on mount (constants.json, summary-stats.json, lembaga-totals.json) and renders the hero stat using the fetched summary. This is the end-to-end vertical slice for the foundation — by the end of this plan, a visitor can load the dev server and see a working hero with no Google CDN.

Purpose: Per D-01/D-03, the POC must be replaced entirely with a minimal scaffold. Per FOUND-06, fonts must be self-hosted. Per FOUND-04, the full token system from UI-SPEC must be promoted to `:global(:root)`. This plan establishes the foundation Plan 02 builds on.

Output: 1 new component file (i18n.js), 1 rewritten component (App.svelte), 1 extended entry point (main.js), 1 modified HTML shell (index.html), 1 deleted POC file (BarChart.svelte), 1 updated dependency manifest (package.json + lock).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-data-pipeline/01-CONTEXT.md
@.planning/phases/01-foundation-data-pipeline/01-PATTERNS.md
@.planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md
@.planning/phases/01-foundation-data-pipeline/SKELETON.md
@dashboard/src/App.svelte
@dashboard/src/main.js
@dashboard/index.html
@dashboard/package.json
@CLAUDE.md

<interfaces>
<!-- Existing JSON file shapes the skeleton will fetch: -->

<!-- /data/summary-stats.json (produced by Plan 03 Task 2): -->
<!--   { totalPagu: int, totalRecords: int, flaggedCount: int, flaggedPagu: int, uniqueLembaga: int, -->
<!--     labelPagu: {low,med,high,unflagged}, labelCounts: {...}, unflaggedPagu: int } -->

<!-- /data/lembaga-totals.json (produced by Plan 03 Task 2): -->
<!--   Array of { rank, name, total, count, ownerType, flaggedCount, flaggedPagu, highCount, medCount, -->
<!--               jenisCounts, metodeCounts, paguByMonth } -->

<!-- /data/constants.json (produced by Plan 03 Task 1): -->
<!--   { apbn: { deficit: {oct2024,fy2025,q1_2026}, unit }, gdp: { konsumsi_pemerintah: {q1_2025..q1_2026}, unit }, sources: [...] } -->

<!-- i18n.js contract (Phase 1 skeleton keys, per UI-SPEC Copywriting Contract): -->
<!--   export const t = { -->
<!--     id: { toggleLabel:'English', scrollCue:'gulir untuk menjelajahi ↓', loading:'memuat data…', -->
<!--           eyebrow:'idsterity · Analisis Pengadaan Indonesia 2026', heroLine1:'Ke mana perginya', -->
<!--           heroLine2:'uang rakyat?', sectionStub:'[bagian sedang disiapkan]', -->
<!--           stepCounter:(n,total)=>`${n} / ${total}` }, -->
<!--     en: { toggleLabel:'Indonesia', scrollCue:'scroll to explore ↓', loading:'loading data…', -->
<!--           eyebrow:'idsterity · Indonesia 2026 Procurement Analysis', heroLine1:'Where did the', -->
<!--           heroLine2:"people's money go?", sectionStub:'[section in preparation]', -->
<!--           stepCounter:(n,total)=>`${n} / ${total}` } -->
<!--   } -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install scrollama + @fontsource dependencies and migrate fonts from CDN to self-hosted (FOUND-06)</name>
  <files>dashboard/package.json, dashboard/package-lock.json, dashboard/src/main.js, dashboard/index.html</files>
  <read_first>
    - dashboard/package.json (current dependencies)
    - dashboard/src/main.js (current mount block — do NOT modify the mount call per PATTERNS.md line 332)
    - dashboard/index.html (lines 7–9 are the Google Fonts links to delete per PATTERNS.md line 391)
    - .planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md §Font Self-Hosting Migration (FOUND-06) — exact list of @fontsource imports
    - .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md §main.js (font import order: Libre Baskerville → Source Serif 4 → JetBrains Mono)
  </read_first>
  <action>
    From the `dashboard/` directory, install four runtime dependencies via npm:
    - `scrollama` (latest 3.x — D-04 specifies "scrollama 3.2"; resolve to ^3.2.0 or current 3.x release)
    - `@fontsource/libre-baskerville`
    - `@fontsource/source-serif-4`
    - `@fontsource/jetbrains-mono`

    Use `npm install --save scrollama @fontsource/libre-baskerville @fontsource/source-serif-4 @fontsource/jetbrains-mono`. Do NOT modify devDependencies or other dependency entries (d3, svelte). Confirm `package-lock.json` is regenerated and committed.

    Edit `dashboard/src/main.js` — prepend five `@fontsource` CSS imports BEFORE the existing `import { mount } from 'svelte'` line, preserving the exact import block order specified in UI-SPEC §Font Self-Hosting Migration:
    - `@fontsource/libre-baskerville/400.css`
    - `@fontsource/libre-baskerville/700.css`
    - `@fontsource/libre-baskerville/400-italic.css`
    - `@fontsource/source-serif-4/400.css`
    - `@fontsource/jetbrains-mono/400.css`

    The Svelte mount block stays as-is — DO NOT touch the `mount(App, { target: document.getElementById('app') })` call.

    Edit `dashboard/index.html` — remove lines 7, 8, 9 (the three Google Fonts `<link>` tags: preconnect to fonts.googleapis.com, preconnect to fonts.gstatic.com, and the css2 stylesheet link). Keep every other line untouched: the global `<style>` reset, `body` font-family fallback chain, `#app` div, and the `<script type="module" src="/src/main.js">` line all remain.

    Update the body font-family fallback in `index.html` to confirm `'Source Serif 4', Georgia, serif` (already present in line 16 of current file — do not change). The body fallback ensures readable text even before fontsource CSS loads.
  </action>
  <verify>
    <automated>cd dashboard && npm install && grep -q '"scrollama"' package.json && grep -q '"@fontsource/libre-baskerville"' package.json && grep -q '"@fontsource/source-serif-4"' package.json && grep -q '"@fontsource/jetbrains-mono"' package.json && grep -c "@fontsource" src/main.js | grep -q '^5$' && ! grep -q 'fonts.googleapis.com' index.html && ! grep -q 'fonts.gstatic.com' index.html && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/package.json` `dependencies` includes `scrollama`, `@fontsource/libre-baskerville`, `@fontsource/source-serif-4`, `@fontsource/jetbrains-mono` (4 new entries)
    - `dashboard/package-lock.json` regenerated and contains resolved entries for all 4 packages
    - `dashboard/src/main.js` contains exactly 5 `@fontsource` import lines, in the order specified above
    - `dashboard/src/main.js` still contains the `mount(App, { target: document.getElementById('app') })` call unchanged
    - `dashboard/index.html` contains no occurrence of `fonts.googleapis.com` or `fonts.gstatic.com` (grep test returns 0)
    - `dashboard/index.html` still contains the `<div id="app"></div>` line and the `<script type="module" src="/src/main.js">` line
    - Verify command exits 0 printing "OK"
  </acceptance_criteria>
  <done>Four runtime dependencies installed; main.js prepended with 5 @fontsource CSS imports preserving the Svelte mount call; index.html has all three Google Fonts CDN links removed; grep-based verify exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Create i18n.js with bilingual skeleton keys and delete POC BarChart.svelte (D-09, D-11)</name>
  <files>dashboard/src/i18n.js, dashboard/src/BarChart.svelte</files>
  <read_first>
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md §D-09 (structure: `export const t = { id: {...}, en: {...} }`), §D-10 (prop-drilling not context API), §D-11 (Indonesian default)
    - .planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md §Copywriting Contract (full table of 8 skeleton keys)
    - .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md §i18n.js (full skeleton code block with all 8 keys)
    - dashboard/src/BarChart.svelte (POC chart — read once to understand what is being discarded; do NOT preserve any content)
  </read_first>
  <action>
    Create `dashboard/src/i18n.js` as a plain ES module with no framework dependency. The module exports a single named const `t`, structured as `{ id: {...}, en: {...} }`.

    Required keys in both `id` and `en` sub-objects (string values unless noted):
    - `toggleLabel` — id: `'English'`, en: `'Indonesia'` (target language label per UI-SPEC §Language Toggle)
    - `scrollCue` — id: `'gulir untuk menjelajahi ↓'`, en: `'scroll to explore ↓'`
    - `loading` — id: `'memuat data…'`, en: `'loading data…'`
    - `eyebrow` — id: `'idsterity · Analisis Pengadaan Indonesia 2026'`, en: `'idsterity · Indonesia 2026 Procurement Analysis'`
    - `heroLine1` — id: `'Ke mana perginya'`, en: `'Where did the'`
    - `heroLine2` — id: `'uang rakyat?'`, en: `"people's money go?"`
    - `sectionStub` — id: `'[bagian sedang disiapkan]'`, en: `'[section in preparation]'`
    - `stepCounter` — function `(n, total) => `${n} / ${total}`` (same implementation in both languages per UI-SPEC table)

    Do NOT add a framework wrapper (no `createI18nStore`, no `writable`, no Svelte runes in this file). Active language state lives in `App.svelte` as a `$state` per D-10. `i18n.js` is a pure data module.

    Delete `dashboard/src/BarChart.svelte` entirely (per D-01: "Delete dashboard/src/App.svelte and dashboard/src/BarChart.svelte entirely"). Use `rm dashboard/src/BarChart.svelte`. Do NOT replace it. Phase 2 will introduce charts. The POC App.svelte deletion happens in Task 3 as part of the rewrite.

    The `i18n.js` file is consumed by `App.svelte` (Task 3) and any child component Phase 2 introduces. No additional exports.
  </action>
  <verify>
    <automated>cd dashboard && test -f src/i18n.js && ! test -f src/BarChart.svelte && node -e "import('./src/i18n.js').then(m=>{const t=m.t;const idKeys=Object.keys(t.id);const enKeys=Object.keys(t.en);const req=['toggleLabel','scrollCue','loading','eyebrow','heroLine1','heroLine2','sectionStub','stepCounter'];for(const k of req){if(!(k in t.id)) throw new Error('missing id.'+k);if(!(k in t.en)) throw new Error('missing en.'+k);}if(t.id.heroLine1!=='Ke mana perginya')throw new Error('id.heroLine1 wrong');if(t.en.heroLine1!=='Where did the')throw new Error('en.heroLine1 wrong');if(typeof t.id.stepCounter!=='function')throw new Error('stepCounter not a function');if(t.id.stepCounter(1,4)!=='1 / 4')throw new Error('stepCounter output wrong');console.log('OK');})"</automated>
  </verify>
  <acceptance_criteria>
    - File `dashboard/src/i18n.js` exists
    - File `dashboard/src/BarChart.svelte` does NOT exist (deleted)
    - `i18n.js` exports a named `t` (grep: `grep -c 'export const t' dashboard/src/i18n.js` returns 1)
    - `t.id` and `t.en` each have all 8 required keys: `toggleLabel`, `scrollCue`, `loading`, `eyebrow`, `heroLine1`, `heroLine2`, `sectionStub`, `stepCounter`
    - `t.id.heroLine1` equals the exact string `Ke mana perginya`
    - `t.en.heroLine1` equals the exact string `Where did the`
    - `t.id.stepCounter` and `t.en.stepCounter` are both functions; calling `stepCounter(1, 4)` returns `'1 / 4'`
    - File contains NO `import` statements from any framework (`grep -c 'from .svelte' dashboard/src/i18n.js` returns 0; `grep -c '$state' dashboard/src/i18n.js` returns 0)
  </acceptance_criteria>
  <done>i18n.js exists as a pure ES module with `t` exporting nested id/en objects containing all 8 skeleton keys; BarChart.svelte deleted; verify command exits 0 printing "OK".</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite App.svelte as Walking Skeleton hero scaffold with fetched data and full token system (FOUND-01, FOUND-04, D-01, D-03)</name>
  <files>dashboard/src/App.svelte</files>
  <read_first>
    - dashboard/src/App.svelte (POC — read fully to extract scroll-cue, hero-stat, loading-pulse, hero h1 CSS patterns; per D-01 this file will be entirely overwritten)
    - .planning/phases/01-foundation-data-pipeline/01-PATTERNS.md §App.svelte (full imports, $state, $derived, fetch-once at mount, hero pattern, loading-pulse pattern, scroll-cue pattern, :global(:root) token block, responsive pattern)
    - .planning/phases/01-foundation-data-pipeline/01-UI-SPEC.md §Typography, §Color, §Spacing Scale, §CSS Token Promotion Checklist (full token list to promote), §Copywriting Contract (id default per D-11)
    - .planning/phases/01-foundation-data-pipeline/01-CONTEXT.md §D-03 (minimal scaffold ONLY — hero + Step container pattern; no S1–S9 placeholder stubs), §D-10 (lang is prop-drilled — declared as $state in App.svelte)
    - .planning/phases/01-foundation-data-pipeline/SKELETON.md (capability proven end-to-end)
  </read_first>
  <action>
    Overwrite `dashboard/src/App.svelte` with a minimal Walking Skeleton scaffold. Do not preserve the POC's S5/S6 placeholder copy or BarChart import. Structure described below — do not inline implementation here; the executor writes idiomatic Svelte 5.

    Imports block:
    - `import { onMount } from 'svelte'` (lifecycle for fetch)
    - `import { t } from './i18n.js'` (bilingual strings)
    - NO `BarChart` import (deleted in Task 2)
    - NO `scrollama` import yet — Plan 02 (Wave 3) adds it

    State block ($state runes, per PATTERNS.md §Svelte 5 Reactive State):
    - `let stats = $state(null)` — populated from /data/summary-stats.json
    - `let lembaga = $state([])` — populated from /data/lembaga-totals.json (kept here so Phase 2 doesn't need to re-add)
    - `let constants = $state(null)` — populated from /data/constants.json
    - `let lang = $state('id')` — per D-11, Indonesian default
    - `let activeStep = $state(0)` — placeholder; Plan 02 wires scrollama against this

    onMount block with Promise.all fetching three JSON files in parallel (extends PATTERNS.md "Fetch-Once at Mount" pattern):
    - `/data/summary-stats.json` → assigned to `stats`
    - `/data/lembaga-totals.json` → assigned to `lembaga`
    - `/data/constants.json` → assigned to `constants`
    No try/catch (PATTERNS.md notes Phase 1 keeps existing silent-failure pattern; Phase 4 adds error handling).
    DO NOT initialize scrollama or IntersectionObserver in this plan — Plan 02 adds it.

    Format helpers (copy from POC App.svelte lines 30–32):
    - `const fmtT = v => (v / 1e12).toFixed(1)`
    - `const fmtNum = v => v.toLocaleString('id-ID')`
    (`fmtPct` from POC can be omitted — not used in skeleton.)

    Template (HTML structure):
    1. Wrapping `<div class="site">`
    2. Section divider comment `<!-- ━━━ HERO ━━━ -->` (per PATTERNS.md §Section Divider Comments)
    3. Hero `<section class="hero">`:
       - Optional `.grain` div (copy SVG noise pattern from POC App.svelte line 226 — Pudding-style texture)
       - `.hero-inner` container with:
         - `.eyebrow` displaying `{t[lang].eyebrow}`
         - `<h1>` with `{t[lang].heroLine1}<br><em>{t[lang].heroLine2}</em>` (the `<em>` retains italic gold accent per UI-SPEC §Color "hero h1 italic")
         - `{#if stats}` block: `.hero-stat` with `.hero-number` reading `Rp {fmtT(stats.totalPagu)} T` and `.hero-sublabel` reading `dialokasikan dalam {fmtNum(stats.totalRecords)} paket` (Indonesian-only for Phase 1 — Phase 2 adds en variant via i18n key)
         - `{:else}` block: `.hero-stat.loading-pulse` showing `Rp — T` and `{t[lang].loading}`
         - `<a class="scroll-cue" href="#story">{t[lang].scrollCue}</a>`
    4. Section divider `<!-- ━━━ SCROLLY (stub for Plan 02) ━━━ -->`
    5. Scrolly stub `<section class="scrolly" id="story">`:
       - `.sticky-col` containing a placeholder `<div class="chart-stub">{t[lang].sectionStub}</div>` and `.step-indicator` rendering 4 pips via `{#each [0,1,2,3] as s} <div class="pip" class:active={activeStep === s}></div> {/each}` (UI-SPEC §Step Pip Indicator)
       - The `.step-indicator` container MUST carry `aria-hidden="true"` (UI-SPEC §Step Pip Indicator: "Pip container must carry aria-hidden")
       - `.steps-col` containing 4 stub steps, each `<div class="step" data-step={N}>` (N = 0..3) with body `<div class="step-card">{t[lang].sectionStub}</div>` and a step-counter span `{t[lang].stepCounter(N+1, 4)}` (placeholder content only — Phase 2 fills S1–S6 here)

    NO language toggle button in this task — Plan 02 (Wave 3) adds it. Plan 02 will need `lang` to be writable, which it already is.

    Styles (`<style>` block):
    Promote the FULL CSS token system from UI-SPEC §CSS Token Promotion Checklist to `:global(:root)` — declared exactly as listed (surfaces, text, accent, data signals, structure, spacing — 9 spacing tokens through `--space-page`, plus `--clean: #3a6b52` which is NEW per PATTERNS.md line 173 and missing from POC).

    Copy these style blocks verbatim from PATTERNS.md (which extracts them from POC App.svelte):
    - `.hero`, `.grain`, `.hero-inner`, `.eyebrow`, `.hero h1`, `.hero h1 em`, `.hero-stat`, `.hero-number`, `.hero-sublabel`
    - `.loading-pulse`, `@keyframes pulse`
    - `.scroll-cue`, `@keyframes bob`
    - `.scrolly`, `.sticky-col`, `.chart-wrap` → renamed to `.chart-stub` with placeholder styling (1px dashed border in `var(--border)`, padded centered text using `var(--muted)`)
    - `.step-indicator`, `.pip`, `.pip.active`
    - `.steps-col`, `.step`, `.step-card`, `.step-num`, `.step-card h3`, `.step-card p`
    - `:global(mark)` and `:global(.c-central|.c-provinsi|.c-kabkota|.c-flagged)` — keep for Phase 2

    Critical changes vs POC:
    - `.sticky-col` height: change `100vh` → `100dvh` (UI-SPEC §Responsive Breakpoints "Use `100dvh` (not `100vh`)")
    - `.hero` height: change `100vh` → `100dvh` (same reasoning)
    - Replace any hardcoded hex inside `<style>` block with `var(--*)` references (UI-SPEC: "All hardcoded hex values currently in component style blocks must be replaced with var(--*) references during Phase 1 cleanup")
    - Add `.step-indicator { display: none; }` inside the existing `@media (max-width: 800px)` block (per PATTERNS.md "pip indicator hidden on mobile" line 219)

    Replace the POC's prose-section ("INTRO PROSE") and closing section entirely — Phase 2 will introduce its own copy. The Phase 1 skeleton ends after the scrolly stub.

    Remove the `BarChart` import and any `<BarChart>` JSX (POC line 98).
  </action>
  <verify>
    <automated>cd dashboard && grep -q "import { t } from './i18n.js'" src/App.svelte && grep -q "let lang = \$state('id')" src/App.svelte && grep -q "let stats = \$state(null)" src/App.svelte && grep -q "let constants = \$state(null)" src/App.svelte && grep -c "fetch('/data/" src/App.svelte | awk '{if($1>=3) exit 0; else exit 1}' && grep -q "100dvh" src/App.svelte && grep -q "\\-\\-clean:" src/App.svelte && grep -q "\\-\\-space-page" src/App.svelte && ! grep -q "import BarChart" src/App.svelte && grep -q 'aria-hidden="true"' src/App.svelte && grep -c "data-step" src/App.svelte | awk '{if($1>=4) exit 0; else exit 1}' && npm run build 2>&1 | grep -q "built in" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `dashboard/src/App.svelte` exists; opens with `<script>` block importing `onMount` from `'svelte'` and `t` from `'./i18n.js'`
    - Does NOT import `BarChart` (grep `'import BarChart'` returns 0)
    - Declares 5 `$state` values: `stats`, `lembaga`, `constants`, `lang` (initial value `'id'`), `activeStep`
    - `onMount` block contains `fetch('/data/summary-stats.json')`, `fetch('/data/lembaga-totals.json')`, `fetch('/data/constants.json')` (all three present)
    - `<style>` block contains `:global(:root)` declaring at minimum: `--bg`, `--text`, `--gold`, `--red`, `--amber`, `--central`, `--provinsi`, `--kabkota`, `--clean`, `--border`, `--space-xs`, `--space-sm`, `--space-md`, `--space-lg`, `--space-xl`, `--space-2xl`, `--space-3xl`, `--space-page` (full UI-SPEC token list)
    - `<style>` block contains `100dvh` (not just `100vh`) for the sticky-col / hero height
    - Template contains `aria-hidden="true"` on the step-indicator container
    - Template contains 4 elements with `data-step` attribute (grep test ≥ 4)
    - Template contains at least one `{t[lang].` reference (i18n is wired)
    - `npm run build` (from `dashboard/`) succeeds with no errors and emits "built in" in stdout
    - `npm run dev` (manual smoke check; not part of verify command but recommended) opens at default Vite port with no console errors
  </acceptance_criteria>
  <done>App.svelte rewritten as minimal hero + scrolly-stub scaffold; full UI-SPEC token system promoted to :global(:root); 3 JSON fetches wired; lang $state declared with id default; 4 data-step stub elements present for Plan 02 to wire scrollama against; npm run build succeeds; verify command exits 0 printing "OK".</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static JSON files → browser JavaScript | Fetched data (`constants.json`, `summary-stats.json`, `lembaga-totals.json`) is parsed and rendered as part of the DOM |
| npm registry → build pipeline | scrollama and @fontsource packages enter the production bundle |
| index.html template → user's browser | The HTML shell loads main.js which loads everything else |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering / XSS | Fetched `lembaga` names rendered in DOM (Phase 2 use) | mitigate | Phase 1 does not yet render `lembaga` names — only fetches and stores them. When Phase 2 renders them, Svelte's default escaping handles HTML entities. This task ensures no `{@html ...}` is used anywhere in App.svelte. Acceptance criteria implicitly enforce via the `aria-hidden` / template structure check. |
| T-01-01-02 | Information disclosure | Source maps / minified bundle | accept | Phase 1 dev build emits source maps by default; Phase 4 controls production build configuration. Source maps reveal i18n key names, not secrets — no API keys in the static site. |
| T-01-01-03 | Tampering | Supply chain — scrollama, @fontsource/* | mitigate | All four new dependencies are well-known open-source packages with active maintenance. `package-lock.json` pins exact versions to prevent floating-version supply chain swaps. Verify task confirms `package-lock.json` is updated. |
| T-01-01-04 | Spoofing | External font loading without integrity | mitigate | Self-hosted fonts via `@fontsource/*` (FOUND-06) eliminate runtime CDN fetches and the risk of compromised fonts.gstatic.com responses. Verify task confirms no `fonts.googleapis.com` or `fonts.gstatic.com` strings remain in `index.html`. |
| T-01-01-05 | Information disclosure | Console errors leaking internal state | accept | No try/catch on fetch (Phase 1 explicit decision per UI-SPEC error-state section); rejected fetches will surface in DevTools console but are not user-visible. Phase 4 adds visible error handling. |
| T-01-01-06 | Denial of service | Large `lembaga-totals.json` blocking render | accept | File is bounded to top-30 institutions (per `TOP_N = 30` in prepare-data.py) — payload is < 50KB. Below DoS threshold. |
</threat_model>

<verification>
1. `cd dashboard && npm install` completes with no errors and `package-lock.json` is updated.
2. `cd dashboard && npm run build` exits 0 (Vite build succeeds with no Svelte compile errors).
3. `cd dashboard && npm run dev` (manual smoke) opens at http://localhost:5173 with no console errors.
4. Browser DevTools Network tab shows zero requests to `fonts.googleapis.com` or `fonts.gstatic.com`.
5. Hero section renders with `Ke mana perginya / uang rakyat?` headline and a real budget figure from `summary-stats.json`.
6. Page source contains the full `:global(:root)` token block from UI-SPEC.
7. `dashboard/src/BarChart.svelte` is deleted (no longer exists).
</verification>

<success_criteria>
- 3 of 9 phase requirement IDs satisfied by this plan (FOUND-01, FOUND-04, FOUND-06)
- Plan 02 (Wave 3) has a working App.svelte to extend with the language toggle button and scrollama wiring
- Walking Skeleton capability "site loads with self-hosted fonts and renders real fetched data" is demonstrably working
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-01-SUMMARY.md`
</output>
