---
phase: 04-polish-share-deploy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/vite.config.js
  - dashboard/src/App.svelte
autonomous: true
requirements:
  - DEPL-01
  - DEPL-02
  - DEPL-03

must_haves:
  truths:
    - "npm run build exits 0 and produces dashboard/dist/ with all assets prefixed /sterity/"
    - "dist/index.html contains script src starting with /sterity/assets/ (not /assets/)"
    - "All 9 safeFetch call sites in App.svelte use import.meta.env.BASE_URL + 'data/...' (no hardcoded /data/)"
    - "In dev (npm run dev), data fetches still resolve correctly — BASE_URL resolves to / in dev mode"
    - "dashboard/dist/ is excluded from git (already confirmed in .gitignore line 7)"
  artifacts:
    - path: "dashboard/vite.config.js"
      provides: "Vite base path config for subdirectory deploy"
      contains: "base: '/sterity/'"
    - path: "dashboard/src/App.svelte"
      provides: "All fetch paths using import.meta.env.BASE_URL"
      contains: "import.meta.env.BASE_URL"
  key_links:
    - from: "dashboard/vite.config.js base: '/sterity/'"
      to: "import.meta.env.BASE_URL in App.svelte"
      via: "Vite static replacement at build time"
      pattern: "base.*'/sterity/'"
    - from: "App.svelte safeFetch calls"
      to: "dashboard/public/data/*.json"
      via: "import.meta.env.BASE_URL + 'data/filename.json'"
      pattern: "import\\.meta\\.env\\.BASE_URL"
---

## Phase Goal

**As a** developer, **I want to** run `npm run build` and get a dist/ that works when FTP'd to /sterity/ on Apache, **so that** all assets and data fetches resolve at the correct subdirectory path without any 404s.

<objective>
Configure Vite base path for subdirectory deploy and replace all hardcoded /data/ fetch paths with import.meta.env.BASE_URL — so the built dist/ works correctly when served from https://yosef.id/sterity/.

Purpose: Without base: '/sterity/' in vite.config.js, the built HTML references /assets/main.js (domain root) instead of /sterity/assets/main.js (subdirectory). Without BASE_URL on fetch calls, data files load in dev but 404 in production.
Output: vite.config.js with base set, App.svelte with 9 call sites fixed, verified npm run build exit 0.
</objective>

<execution_context>
@/Users/yosef/Projects/idsterity/.planning/phases/04-polish-share-deploy/execute-plan.md
@/Users/yosef/Projects/idsterity/.planning/phases/04-polish-share-deploy/summary.md
</execution_context>

<context>
@/Users/yosef/Projects/idsterity/.planning/ROADMAP.md
@/Users/yosef/Projects/idsterity/.planning/REQUIREMENTS.md

<interfaces>
<!-- Current dashboard/vite.config.js (lines 1–6): -->
<!--
  import { defineConfig } from 'vite'
  import { svelte } from '@sveltejs/vite-plugin-svelte'

  export default defineConfig({
    plugins: [svelte()]
  })
-->
<!-- Add base: '/sterity/' before plugins. Both leading and trailing slash required. -->

<!-- safeFetch definition in App.svelte (lines 110–114) — DO NOT MODIFY: -->
<!--
  const safeFetch = url =>
    fetch(url).then(r => {
      if (!r.ok) throw new Error(`${r.status} ${r.statusText} — ${url}`)
      return r.json()
    })
-->

<!-- 9 call sites to fix in App.svelte: -->
<!-- Group 1 — setFilter function (lines 65, 67, 69): -->
<!--   safeFetch('/data/wordcloud-all.json')      → safeFetch(import.meta.env.BASE_URL + 'data/wordcloud-all.json') -->
<!--   safeFetch('/data/wordcloud-central.json')  → safeFetch(import.meta.env.BASE_URL + 'data/wordcloud-central.json') -->
<!--   safeFetch('/data/wordcloud-district.json') → safeFetch(import.meta.env.BASE_URL + 'data/wordcloud-district.json') -->
<!-- Group 2 — selectWord function (line 93, template literal): -->
<!--   safeFetch(`/data/word-${word}-${filterKey}.json`) → safeFetch(import.meta.env.BASE_URL + `data/word-${word}-${filterKey}.json`) -->
<!-- Group 3 — onMount Promise.all (lines 119–123): -->
<!--   safeFetch('/data/summary-stats.json')       → safeFetch(import.meta.env.BASE_URL + 'data/summary-stats.json') -->
<!--   safeFetch('/data/lembaga-totals.json')      → safeFetch(import.meta.env.BASE_URL + 'data/lembaga-totals.json') -->
<!--   safeFetch('/data/constants.json')           → safeFetch(import.meta.env.BASE_URL + 'data/constants.json') -->
<!--   safeFetch('/data/wordcloud-all.json')       → safeFetch(import.meta.env.BASE_URL + 'data/wordcloud-all.json') -->
<!--   safeFetch('/data/wordcloud-lembaga.json')   → safeFetch(import.meta.env.BASE_URL + 'data/wordcloud-lembaga.json') -->

<!-- Replacement rule: -->
<!--   String literals: '/data/ → import.meta.env.BASE_URL + 'data/ -->
<!--   Template literals: `/data/ → import.meta.env.BASE_URL + `data/ -->
<!--   NO leading slash on 'data/...' — Vite guarantees BASE_URL ends with '/' -->
<!--   NEVER use import.meta.env['BASE_URL'] (bracket notation) — Vite does not replace it -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add base path to vite.config.js and fix all 9 safeFetch call sites in App.svelte</name>
  <files>dashboard/vite.config.js, dashboard/src/App.svelte</files>
  <read_first>
    - dashboard/vite.config.js (lines 1–6, confirm current structure before edit)
    - dashboard/src/App.svelte (lines 58–130, confirm all call site locations before edit)
  </read_first>
  <action>
    Step 1 — Edit dashboard/vite.config.js:
    Add base: '/sterity/' to the defineConfig options object, placed above plugins. The base value MUST have both a leading and trailing slash. The result is:
      defineConfig({ base: '/sterity/', plugins: [svelte()] })

    Step 2 — Edit dashboard/src/App.svelte, 9 call sites:

    setFilter function (lines 65, 67, 69) — replace string arguments:
    - '/data/wordcloud-all.json' → import.meta.env.BASE_URL + 'data/wordcloud-all.json'
    - '/data/wordcloud-central.json' → import.meta.env.BASE_URL + 'data/wordcloud-central.json'
    - '/data/wordcloud-district.json' → import.meta.env.BASE_URL + 'data/wordcloud-district.json'

    selectWord function (line 93) — template literal call site:
    - `/data/word-${word}-${filterKey}.json` → import.meta.env.BASE_URL + `data/word-${word}-${filterKey}.json`
    (Note: no leading slash on data/ in the template literal — BASE_URL ends with /)

    onMount Promise.all (lines 119–123) — replace all five string arguments:
    - '/data/summary-stats.json' → import.meta.env.BASE_URL + 'data/summary-stats.json'
    - '/data/lembaga-totals.json' → import.meta.env.BASE_URL + 'data/lembaga-totals.json'
    - '/data/constants.json' → import.meta.env.BASE_URL + 'data/constants.json'
    - '/data/wordcloud-all.json' → import.meta.env.BASE_URL + 'data/wordcloud-all.json'
    - '/data/wordcloud-lembaga.json' → import.meta.env.BASE_URL + 'data/wordcloud-lembaga.json'

    Do NOT modify the safeFetch function definition (lines 110–114). Do NOT modify any error handling catch blocks. Do NOT use bracket notation import.meta.env['BASE_URL'] anywhere.

    After all edits, verify no hardcoded '/data/' string literals remain in App.svelte by running:
      grep -n "safeFetch('/data/" dashboard/src/App.svelte
    Expected output: empty (no matches).

    Also verify no template literal with leading slash remains:
      grep -n 'safeFetch(`/data/' dashboard/src/App.svelte
    Expected output: empty (no matches).
  </action>
  <verify>
    <automated>grep -c "base: '/sterity/'" dashboard/vite.config.js && grep -c "import.meta.env.BASE_URL" dashboard/src/App.svelte</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "base: '/sterity/'" dashboard/vite.config.js returns 1
    - grep -c "import.meta.env.BASE_URL" dashboard/src/App.svelte returns 9
    - grep "safeFetch('/data/" dashboard/src/App.svelte returns empty (no hardcoded /data/ string literals)
    - grep "safeFetch(\`/data/" dashboard/src/App.svelte returns empty (no hardcoded /data/ template literals)
    - grep "import.meta.env\['BASE_URL'\]" dashboard/src/App.svelte returns empty (no bracket notation)
    - The safeFetch function definition at lines 110–114 is unchanged (fetch(url).then...)
  </acceptance_criteria>
  <done>vite.config.js has base: '/sterity/' and App.svelte has 9 BASE_URL call sites — no remaining hardcoded /data/ paths</done>
</task>

<task type="auto">
  <name>Task 2: Run npm run build and verify dist/ structure</name>
  <files>dashboard/dist/</files>
  <read_first>
    - dashboard/vite.config.js (confirm base: '/sterity/' present before building)
  </read_first>
  <action>
    Run npm run build from the dashboard/ directory. Verify the build succeeds and the output structure is correct for subdirectory deploy.

    Run these checks after the build exits 0:

    Check 1 — asset paths prefixed with /sterity/:
      grep -o 'src="/sterity/[^"]*"' dashboard/dist/index.html
    Expected: at least one script tag with src="/sterity/assets/..." (not "/assets/...")

    Check 2 — no unhashed asset paths pointing to domain root:
      grep 'src="/assets/' dashboard/dist/index.html
    Expected: empty output (no bare /assets/ references)

    Check 3 — dist/ excluded from git (DEPL-03 already satisfied at .gitignore line 7):
      grep 'dashboard/dist' .gitignore
    Expected: dashboard/dist/ on line 7

    Check 4 — data files still present in dist/:
      ls dashboard/dist/data/
    Expected: summary-stats.json, lembaga-totals.json, constants.json, wordcloud-*.json, word-*.json present

    If the build fails, read the error output. Common cause: syntax error introduced during Task 1 edits. Fix the offending line in App.svelte and re-run.
  </action>
  <verify>
    <automated>cd /Users/yosef/Projects/idsterity/dashboard && npm run build && grep -c '/sterity/assets/' dist/index.html</automated>
  </verify>
  <acceptance_criteria>
    - npm run build exits with code 0
    - grep -c '/sterity/assets/' dashboard/dist/index.html returns at least 1
    - grep 'src="/assets/' dashboard/dist/index.html returns empty (no unhashed domain-root asset paths)
    - ls dashboard/dist/data/ shows the data JSON files present
    - git check-ignore -v dashboard/dist/ reports dashboard/.gitignore:7:dashboard/dist/ (DEPL-03 confirmed)
  </acceptance_criteria>
  <done>npm run build exits 0, dist/ assets all reference /sterity/ prefix, data files present in dist/data/, dist/ excluded from git</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /sterity/data/*.json | Browser fetches pre-baked static JSON via import.meta.env.BASE_URL path; no user input in URL construction except word/filterKey which are sourced from cloudWords (server-controlled) |
| Vite build → dist/ | Build-time static replacement; no runtime secrets; output is public static files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | import.meta.env.BASE_URL path construction | accept | word and filterKey values in template literal (line 93) are sourced from cloudWords array (server-controlled JSON), not from URL params or user text input — no path traversal possible |
| T-04-05 | Information Disclosure | dist/ build output in git | accept | DEPL-03 confirmed satisfied — dashboard/dist/ in .gitignore line 7; dist/ will not be committed |
| T-04-06 | Denial of Service | safeFetch error handling | accept | safeFetch errors surface as UI error states (filterError, wordRecordsError) — no crash, graceful degradation; ASVS L1 acceptable |
</threat_model>

<verification>
1. grep -c "base: '/sterity/'" dashboard/vite.config.js returns 1
2. grep -c "import.meta.env.BASE_URL" dashboard/src/App.svelte returns 9
3. grep "safeFetch('/data/" dashboard/src/App.svelte returns empty
4. cd dashboard && npm run build exits 0
5. grep -c '/sterity/assets/' dashboard/dist/index.html returns at least 1
6. ls dashboard/dist/data/ shows data JSON files present
7. git check-ignore -v dashboard/dist/ confirms gitignore match
</verification>

<success_criteria>
- vite.config.js has base: '/sterity/' (DEPL-01 — build produces correct subdirectory paths)
- All 9 safeFetch call sites use import.meta.env.BASE_URL (DEPL-02)
- dist/ excluded from git at .gitignore line 7 (DEPL-03 — already satisfied, confirmed)
- npm run build exits 0 and dist/index.html references /sterity/assets/ paths
</success_criteria>

<output>
After completion, create .planning/phases/04-polish-share-deploy/04-02-SUMMARY.md
</output>
