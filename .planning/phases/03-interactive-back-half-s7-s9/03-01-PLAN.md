---
phase: 03-interactive-back-half-s7-s9
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - dashboard/public/data/constants.json
  - dashboard/src/i18n.js
  - dashboard/src/App.svelte
autonomous: true
requirements: [SEC-07]
tags: [svelte5, scrollama, animation, count-up, raf, i18n]
must_haves:
  truths:
    - "User scrolling into S7 Step 0 sees two count-up animations (kopi jago cups + seblak portions) animate from 0 to final values in ~1.8s in gold (--gold)"
    - "User scrolling into S7 Step 1 first sees a bilingual aria-live transition label appear ('Atau, lebih seriusnya…' / 'Or, on a more serious note…'), then sees SD schools count-up start ~300ms later and puskesmas count-up start ~450ms later, both rendered in red (--red)"
    - "All four count-up final values are derived from stats.labelPagu.high divided by constants.anchors.{kopi|seblak|sd|puskesmas}.price"
    - "Each anchor in the sticky panel shows label (--text-body, --muted) and citation line (--text-micro, --muted) below the count number"
    - "Anchor numbers respect prefers-reduced-motion by snapping to final value with no RAF loop"
    - "All S7 copy renders in the active language and the bilingual language toggle preserves scroll position"
  artifacts:
    - path: "dashboard/public/data/constants.json"
      provides: "Top-level `anchors` key with kopi/seblak/sd/puskesmas entries; each entry has price (IDR int), label_id, label_en, source URL, sourceLabel"
      contains: "\"anchors\""
    - path: "dashboard/src/i18n.js"
      provides: "S7 bilingual keys in both id and en blocks: s7Eyebrow, s7StickyHeading, s7Step0Heading, s7Step0Body, s7KopiLabel, s7SeblakLabel, s7TransitionLabel, s7Step1Heading, s7Step1Body, s7SDLabel, s7PuskesmasLabel, s7SourcePrefix"
      contains: "s7Eyebrow"
    - path: "dashboard/src/App.svelte"
      provides: "S7 section HTML with data-section=\"s7\" and two data-step divs, $state variables (activeStepS7, kopiCount, seblakCount, sdCount, puskesmasCount, s7ShowTransition), makeScroller('s7') call, countUp() RAF utility, and S7 CSS"
      contains: "data-section=\"s7\""
  key_links:
    - from: "dashboard/src/App.svelte onMount Promise.all"
      to: "dashboard/public/data/constants.json (anchors key)"
      via: "existing safeFetch('/data/constants.json') — no new fetch needed"
      pattern: "constants\\.anchors\\.(kopi|seblak|sd|puskesmas)\\.price"
    - from: "scrollama onStepEnter for s7"
      to: "kopiCount/seblakCount/sdCount/puskesmasCount $state via countUp() RAF callback"
      via: "$effect or direct call in onStepEnter; activeStepS7 transitions trigger animations"
      pattern: "countUp\\("
    - from: "S7 sticky-col anchor figures"
      to: "stats.labelPagu.high (from existing summary-stats.json)"
      via: "Math.floor(stats.labelPagu.high / constants.anchors.{anchor}.price)"
      pattern: "labelPagu\\.high"
---

## Phase Goal

**As a** site visitor who has just absorbed the deficit and institutions narrative, **I want to** see the AI-flagged Rp 10.7T translated into kopi jago cups, seblak portions, elementary schools, and puskesmas — animated as I scroll through two beats (absurd then sobering) — **so that** the scale of inappropriate procurement lands viscerally instead of staying as an abstract trillion-rupiah number.

<objective>
Deliver the S7 Anchor Count-Up vertical slice as a complete, scroll-driven, bilingual feature wired end-to-end. The slice spans three layers in a single plan: (1) static data — add `anchors` key with researcher-determined unit prices and source citations to `constants.json`; (2) copy — add 12 bilingual S7 keys to `i18n.js`; (3) interactive UI — add the S7 `<section>` to `App.svelte` with two scroll steps, a `countUp()` RAF utility, four `$state` count variables, an `aria-live` transition label, and scoped CSS using existing tokens.

Purpose: SEC-07 — animate "absurd" spending as count-up reveals per anchor, both languages, cited unit prices. This is the visceral payoff after the analytical S5/S6 institutions chart.

Output: A scrollable S7 section in the dev server that fires four count-up animations driven by `stats.labelPagu.high` and `constants.anchors.*` (no new fetches added — extends existing onMount Promise.all triple).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-interactive-back-half-s7-s9/03-CONTEXT.md
@.planning/phases/03-interactive-back-half-s7-s9/03-RESEARCH.md
@.planning/phases/03-interactive-back-half-s7-s9/03-PATTERNS.md
@.planning/phases/03-interactive-back-half-s7-s9/03-UI-SPEC.md
@dashboard/src/App.svelte
@dashboard/src/i18n.js
@dashboard/public/data/constants.json
@dashboard/public/data/summary-stats.json

<interfaces>
<!-- Key contracts the executor needs. Extracted from existing code. -->

From dashboard/src/App.svelte (existing patterns to extend, lines referenced for read):
- Reactive state declared at top of <script>: `let stats = $state(null)`, `let constants = $state(null)`, `let lang = $state('id')`, `let activeStepS1..S5 = $state(0)`, `let fetchError = $state(null)`.
- `safeFetch(url)` returns parsed JSON or throws. Existing onMount Promise.all triple loads summary-stats, lembaga-totals, constants in parallel — extend nothing here (no new fetches for S7).
- `makeScroller(sectionAttr, onEnter)` (lines 51–56) creates a scrollama instance bound to `[data-section="${sectionAttr}"] [data-step]`. The `scrollers = [...]` array (lines 57–62) is where new scrollers are added; `onDestroy` already iterates this array.
- Mobile/desktop offset already computed once in onMount: `const isMobile = window.matchMedia('(max-width: 800px)').matches; const offset = isMobile ? 0.1 : 0.5`. Reuse — do not redeclare.
- Number formatting: `const fmtNum = v => v.toLocaleString('id-ID')` at line 73. S7 needs a locale-aware variant for English — add `const fmtCount = (v, lang) => v.toLocaleString(lang === 'id' ? 'id-ID' : 'en-US')`.
- Bilingual copy access: `{t[lang].keyName}` — never inline ternaries.
- aria-live transition label exact pattern from S5/S6 (App.svelte lines 369–371): `<span class="s6-transition-label" aria-live="polite">{activeStepS5 >= 2 ? t[lang].s6TransitionLabel : ''}</span>`.

From dashboard/public/data/summary-stats.json (verified):
```json
{
  "labelPagu": { "low": ..., "med": ..., "high": 10729224542293, "unflagged": ... },
  ...
}
```
`stats.labelPagu.high` is an integer in IDR; division by anchor price yields the count (Math.floor).

From dashboard/public/data/constants.json (top-level keys after this plan):
```
apbn, gdp, sources, anchors (NEW)
```
The `anchors` value shape (researcher-recommended values from RESEARCH.md §Anchor Unit Prices, MEDIUM/LOW confidence — values may be adjusted by executor if better citations are found, but the four anchor keys and field names are LOCKED):
```json
{
  "kopi":      { "price": 8000,         "label_id": "cangkir kopi jago",   "label_en": "cups of kopi jago",          "source": "...", "sourceLabel": "..." },
  "seblak":    { "price": 15000,        "label_id": "porsi seblak",        "label_en": "portions of seblak",         "source": "...", "sourceLabel": "..." },
  "sd":        { "price": 4500000000,   "label_id": "sekolah dasar baru",  "label_en": "new elementary schools",     "source": "...", "sourceLabel": "..." },
  "puskesmas": { "price": 8000000000,   "label_id": "puskesmas baru",      "label_en": "new primary health clinics", "source": "...", "sourceLabel": "..." }
}
```
Recommended source URLs and labels in RESEARCH.md §Recommended `constants.json` `anchors` Key Structure. The label_id/label_en fields are FALLBACKS — the displayed anchor labels come from i18n keys (s7KopiLabel etc), but label_id/label_en are kept in the data file for traceability with the source citation.

From dashboard/src/i18n.js (existing structure, lines 1–162):
- Two top-level objects: `t.id` and `t.en`. Identical keys, different values. Both blocks must receive the same 12 new S7 keys.
- Section divider comments: `// S1`, `// S2 — APBN Deficit`, ..., `// S5 — Top Institutions`, `// S6 — Re-rank`. Add `// S7 — Anchor Count-Up` divider before the S7 block in both id and en sections.
- All keys are string-valued except `stepCounter: (n, total) => `${n} / ${total}`` (function). S7 keys are all plain strings.

From UI-SPEC §Copywriting Contract S7 — exact bilingual strings for all 12 S7 keys are LOCKED:
- s7Eyebrow:           id="S7 · ANGKA NYATA"                                                          en="S7 · REAL NUMBERS"
- s7StickyHeading:     id="Rp 10,7 triliun untuk pengadaan bermasalah"                                en="Rp 10.7 trillion for inappropriate procurement"
- s7Step0Heading:      id="Bayangkan ini…"                                                            en="Imagine this…"
- s7Step0Body:         id="Rp 10,7 triliun — total anggaran yang dinilai AI bermasalah. Angka itu besar. Tapi seberapa besar?"   en="Rp 10.7 trillion — the total budget flagged by AI as inappropriate. That's a large number. But how large?"
- s7KopiLabel:         id="cangkir kopi jago"                                                         en="cups of kopi jago"
- s7SeblakLabel:       id="porsi seblak"                                                              en="portions of seblak"
- s7TransitionLabel:   id="Atau, lebih seriusnya…"                                                    en="Or, on a more serious note…"
- s7Step1Heading:      id="Atau, bangun ini."                                                         en="Or, build this instead."
- s7Step1Body:         id="Dana yang sama bisa membangun ribuan sekolah dasar baru — atau lebih dari seribu puskesmas. Tapi tidak digunakan untuk itu."   en="The same funds could build thousands of new elementary schools — or over a thousand primary health clinics. But they weren't used for that."
- s7SDLabel:           id="sekolah dasar baru"                                                        en="new elementary schools"
- s7PuskesmasLabel:    id="puskesmas baru"                                                            en="new primary health clinics"
- s7SourcePrefix:      id="Harga satuan: "                                                            en="Unit price: "

From UI-SPEC §Section Layout Contracts S7 (locked):
- Two-column scrolly section, same skeleton as S5 (`<section class="scrolly" data-section="s7" id="s7">` with `.sticky-col` and `.steps-col`).
- Step 0 (data-step="0"): sticky panel shows `.s7-anchor-pair` flex column (`gap: var(--space-xl)`) with two `.s7-anchor` items — kopi then seblak. Each item is `count` figure (`--text-display` ≈ `clamp(2.2rem, 5vw, 3.8rem)`, weight 700, Libre Baskerville, color `var(--gold)`) + label (`--text-body` 0.9rem, `var(--muted)`) + citation line (`--text-micro` 0.7rem, JetBrains Mono, `var(--muted)`).
- Step 1 (data-step="1"): same `.s7-anchor-pair` but anchors are SD + puskesmas; count figures render in `var(--red)` not `var(--gold)`. The `.s7-transition-label` (`--text-micro`, JetBrains Mono, italic, `var(--muted)`, `aria-live="polite"`) appears above the pair when `s7ShowTransition` becomes true.
- Step indicator: 2 pips, `aria-hidden="true"`, desktop only — same `.step-indicator` / `.pip` classes already in App.svelte CSS.
- Step cards in `.steps-col`: one card per `data-step` div containing `step-num` (e.g. `t[lang].stepCounter(1, 2)`), `h3` (heading), `p` (body), and an `a.source-link` per anchor citation.

From UI-SPEC §Interaction Contracts S7 (locked):
- Step 0 entry: kopiCount and seblakCount countUps fire simultaneously, duration 1800ms, ease-out cubic.
- Step 1 entry: s7ShowTransition = true immediately; setTimeout 300ms → sdCount countUp; setTimeout 450ms → puskesmasCount countUp; both same duration/easing; figures in --red.
- Backward scroll: scrollama re-entry resets activeStepS7 → effect re-runs → counts re-animate from 0.
- prefers-reduced-motion: skip RAF, set to final value immediately.

From UI-SPEC §Color (locked):
- Step 0 count-up figure color: `var(--gold)` (#c9a84c)
- Step 1 count-up figure color: `var(--red)` (#c44242)
- Anchor label: `var(--muted)`. Citation line: `var(--muted)`.
- Transition label: `var(--muted)`.
- All other text uses existing tokens.

From UI-SPEC §Spacing (locked):
- `.s7-anchor-pair` gap: `var(--space-xl)` (32px)
- `.s7-anchor` internal layout: figure → label → citation, `gap: var(--space-xs)`
- Section vertical padding: `var(--space-3xl)` (64px)
- Step card padding: `var(--space-lg)` (24px) — same as existing `.step-card`

</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add `anchors` key + 4 source entries to constants.json</name>
  <files>dashboard/public/data/constants.json</files>
  <read_first>
    - dashboard/public/data/constants.json (full file — current top-level keys are `apbn`, `gdp`, `sources`; preserve all existing content; new key goes at same top level after `sources`)
    - .planning/phases/03-interactive-back-half-s7-s9/03-RESEARCH.md §Anchor Unit Prices — Research Findings AND §Recommended `constants.json` `anchors` Key Structure (recommended IDR values, source URLs, sourceLabels for kopi/seblak/sd/puskesmas)
    - .planning/phases/03-interactive-back-half-s7-s9/03-CONTEXT.md D-02 (researcher-determined prices, each with `sources` entry)
  </read_first>
  <action>
    Add a new top-level `anchors` key to `dashboard/public/data/constants.json` between the existing `sources` array and the closing `}`. The `anchors` object MUST contain exactly four child keys — `kopi`, `seblak`, `sd`, `puskesmas` — in that order. Each child is an object with exactly five fields in this order: `price` (IDR integer, no decimals, no separators), `label_id` (Indonesian noun phrase), `label_en` (English noun phrase), `source` (full https URL), `sourceLabel` (publisher-attributed citation string). Use the RESEARCH.md recommended values verbatim (kopi: 8000, seblak: 15000, sd: 4500000000, puskesmas: 8000000000) and the four recommended source URLs from RESEARCH.md §Recommended `constants.json` `anchors` Key Structure. Preserve every existing line of the file unchanged. Maintain 2-space indentation matching the rest of the file. The file MUST remain valid JSON (run `python3 -m json.tool` mentally or via jq to confirm).

    If a price feels unsupported by the listed source on second reading, the executor may adjust the IDR value AND swap source/sourceLabel together for a more credible citation — but must NOT remove or rename the four child keys. The four anchor keys are LOCKED per D-02.
  </action>
  <verify>
    <automated>cd /Users/yosef/Projects/idsterity &amp;&amp; python3 -c "import json; d=json.load(open('dashboard/public/data/constants.json')); a=d['anchors']; assert set(a.keys())=={'kopi','seblak','sd','puskesmas'}, f'keys: {set(a.keys())}'; [assert_(set(a[k].keys())=={'price','label_id','label_en','source','sourceLabel'}) for k in a] if False else None; missing=[k for k in a if set(a[k].keys())!={'price','label_id','label_en','source','sourceLabel'}]; assert not missing, f'bad shape: {missing}'; bad=[k for k in a if not isinstance(a[k]['price'],int) or a[k]['price']&lt;=0]; assert not bad, f'bad prices: {bad}'; nohttp=[k for k in a if not a[k]['source'].startswith('https://')]; assert not nohttp, f'non-https: {nohttp}'; print('OK', list(a.keys()))"</automated>
  </verify>
  <acceptance_criteria>
    - `constants.json` parses as valid JSON
    - Top-level `anchors` key exists with exactly four child keys: `kopi`, `seblak`, `sd`, `puskesmas`
    - Each anchor object has exactly five fields: `price` (int), `label_id` (string), `label_en` (string), `source` (https URL), `sourceLabel` (string)
    - Existing `apbn`, `gdp`, `sources` keys present and unchanged (diff shows only additions, no deletions in those keys)
    - All four `source` values begin with `https://`
    - All four `price` values are positive integers
  </acceptance_criteria>
  <done>constants.json contains the `anchors` key with all four anchors fully populated and the existing apbn/gdp/sources content untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add 12 S7 bilingual keys to i18n.js</name>
  <files>dashboard/src/i18n.js</files>
  <read_first>
    - dashboard/src/i18n.js (full file — both `t.id` and `t.en` blocks must receive the same 12 keys with identical names)
    - .planning/phases/03-interactive-back-half-s7-s9/03-UI-SPEC.md §Copywriting Contract — S7 — Anchor Count-Up (exact bilingual strings for all 12 keys are LOCKED; do not paraphrase)
    - .planning/phases/03-interactive-back-half-s7-s9/03-PATTERNS.md §`dashboard/src/i18n.js` (section divider comment convention, key naming pattern, parallel id/en structure)
  </read_first>
  <action>
    Extend `dashboard/src/i18n.js` with the S7 i18n keys. Add a `// S7 — Anchor Count-Up` section divider comment in both the `t.id` block and the `t.en` block, positioned immediately after the existing `// S6 — Re-rank` block and before the `// Errors` block. Under the new S7 divider, add exactly these 12 keys in this order: `s7Eyebrow`, `s7StickyHeading`, `s7Step0Heading`, `s7Step0Body`, `s7KopiLabel`, `s7SeblakLabel`, `s7TransitionLabel`, `s7Step1Heading`, `s7Step1Body`, `s7SDLabel`, `s7PuskesmasLabel`, `s7SourcePrefix`. Indonesian strings go in `t.id`; English strings go in `t.en`. Use the verbatim strings from UI-SPEC §Copywriting Contract S7 (no paraphrasing — special characters including em-dashes `—`, ellipsis `…`, Indonesian comma decimals "Rp 10,7", English period decimals "Rp 10.7" must be preserved exactly). Maintain the existing 4-space indentation inside the object and the alignment style used in nearby keys (colon-aligned values where present, but minor whitespace drift is acceptable — do not reformat existing keys).
  </action>
  <verify>
    <automated>cd /Users/yosef/Projects/idsterity/dashboard &amp;&amp; node --input-type=module -e "import('./src/i18n.js').then(m=&gt;{const k=['s7Eyebrow','s7StickyHeading','s7Step0Heading','s7Step0Body','s7KopiLabel','s7SeblakLabel','s7TransitionLabel','s7Step1Heading','s7Step1Body','s7SDLabel','s7PuskesmasLabel','s7SourcePrefix'];for(const lang of ['id','en']){const miss=k.filter(x=&gt;!m.t[lang][x]);if(miss.length){console.error('MISSING',lang,miss);process.exit(1)}}console.log('OK 12 keys × 2 langs')})"</automated>
  </verify>
  <acceptance_criteria>
    - `i18n.js` exports `t` with both `t.id` and `t.en` containing all 12 S7 keys (s7Eyebrow, s7StickyHeading, s7Step0Heading, s7Step0Body, s7KopiLabel, s7SeblakLabel, s7TransitionLabel, s7Step1Heading, s7Step1Body, s7SDLabel, s7PuskesmasLabel, s7SourcePrefix)
    - Every S7 key in `t.id` has a non-empty Indonesian string value; every S7 key in `t.en` has a non-empty English string value
    - `t.id.s7TransitionLabel === 'Atau, lebih seriusnya…'` (UTF-8 ellipsis character, exact match)
    - `t.en.s7TransitionLabel === 'Or, on a more serious note…'`
    - `t.id.s7StickyHeading` contains the substring `Rp 10,7 triliun` (Indonesian comma decimal)
    - `t.en.s7StickyHeading` contains the substring `Rp 10.7 trillion` (English period decimal)
    - Existing S1–S6 + hero + errors keys are present and string-equal to their pre-edit values (no accidental edits)
    - `// S7 — Anchor Count-Up` divider comment present in both t.id and t.en blocks
  </acceptance_criteria>
  <done>i18n.js exports both id and en variants with all 12 S7 keys populated verbatim from UI-SPEC and existing keys untouched.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire S7 section into App.svelte (state, scrollama, countUp utility, HTML, CSS)</name>
  <files>dashboard/src/App.svelte</files>
  <read_first>
    - dashboard/src/App.svelte (full file — extend the existing single-file Svelte component; new state declarations go in the `<script>` block alongside `activeStepS1..S5`; new scroller goes into the `scrollers = [...]` array; new section HTML goes inside `<div class="site">` immediately AFTER the existing S5 `<section class="scrolly" data-section="s5">` block and BEFORE the `{#if fetchError}` line; new CSS goes inside the existing scoped `<style>` block)
    - .planning/phases/03-interactive-back-half-s7-s9/03-PATTERNS.md (exact analog patterns for state, makeScroller, aria-live transition label, source-link, mobile sticky behavior, step-indicator pips, stats/constants guard pattern)
    - .planning/phases/03-interactive-back-half-s7-s9/03-RESEARCH.md §Pattern 2 (countUp RAF loop implementation with prefers-reduced-motion guard) AND §Pattern 3 (S7 step-driven $effect)
    - .planning/phases/03-interactive-back-half-s7-s9/03-UI-SPEC.md §Section Layout Contracts — S7 (HTML structure, two beats, transition label timing) AND §Interaction Contracts S7 (1800ms duration, 300ms/450ms staggers, cubic ease-out, color contracts gold→red)
    - dashboard/src/i18n.js (S7 keys added in Task 2 — confirm naming before referencing in HTML)
    - dashboard/public/data/constants.json (anchors key added in Task 1 — confirm field names `kopi.price`, `seblak.price`, `sd.price`, `puskesmas.price` before reading in code)
  </read_first>
  <action>
    Extend `dashboard/src/App.svelte` in four layers, in this order:

    **A. Reactive state** — Add to the `<script>` block, immediately after `let activeStepS5 = $state(0)`, the following new `$state` declarations: `activeStepS7` (init 0), `kopiCount` (init 0), `seblakCount` (init 0), `sdCount` (init 0), `puskesmasCount` (init 0), `s7ShowTransition` (init false). Also add a `let s7Timers = []` plain variable (not `$state` — used to track outstanding setTimeout handles for cleanup).

    **B. Utility functions** — In the `<script>` block, after the existing `fmtT` / `fmtNum` lines and before `toggleLang`, add: (1) `fmtCount(v, lang)` returning `v.toLocaleString(lang === 'id' ? 'id-ID' : 'en-US')`; (2) a `countUp(target, duration, onUpdate, onDone)` function matching RESEARCH.md Pattern 2 — RAF loop, eased via `1 - Math.pow(1 - t, 3)` (cubic ease-out approximating the locked `cubic-bezier(0.33, 1, 0.68, 1)`), with a `prefers-reduced-motion` short-circuit that calls `onUpdate(target)` and `onDone?.()` immediately. The function takes a target integer count, a duration in milliseconds (callers pass 1800), an onUpdate(currentInt) callback, and an optional onDone() callback.

    **C. Scrollama wiring + step-driven effect** — Inside the existing `requestAnimationFrame(...)` inner closure of `onMount`, append `makeScroller('s7', i => { activeStepS7 = i })` to the `scrollers = [...]` array (it must remain inside the same `scrollers = [...]` literal). Then add a `$effect(() => { ... })` block at the bottom of the `<script>` (after `toggleLang`) that reacts to `activeStepS7`, `stats`, and `constants`:
      - Guard: if `!stats || !constants?.anchors` return.
      - Clear pending timers: `s7Timers.forEach(clearTimeout); s7Timers = []`.
      - Compute `high = stats.labelPagu.high ?? 0`.
      - When `activeStepS7 === 0`: set `s7ShowTransition = false`; reset `sdCount = 0; puskesmasCount = 0;` (so backward scroll re-arms Step 1); call `countUp(Math.floor(high / constants.anchors.kopi.price), 1800, v => kopiCount = v)` and `countUp(Math.floor(high / constants.anchors.seblak.price), 1800, v => seblakCount = v)` simultaneously.
      - When `activeStepS7 === 1`: set `s7ShowTransition = true`; reset `sdCount = 0; puskesmasCount = 0`; push two `setTimeout` handles into `s7Timers` — one at 300ms firing `countUp(Math.floor(high / constants.anchors.sd.price), 1800, v => sdCount = v)`, another at 450ms firing `countUp(Math.floor(high / constants.anchors.puskesmas.price), 1800, v => puskesmasCount = v)`.
    In `onDestroy`, also clear any pending timers: `s7Timers.forEach(clearTimeout)`.

    **D. HTML section** — Insert a new `<section class="scrolly" data-section="s7" id="s7">` block in the template immediately AFTER the existing S5 section (after its closing `</section>` and BEFORE the `{#if fetchError}` block). Skeleton: a `.sticky-col` with `eyebrow` div (`{t[lang].s7Eyebrow}`), a heading `<h2 class="s7-sticky-heading">{t[lang].s7StickyHeading}</h2>`, a conditional `<span class="s7-transition-label" aria-live="polite">{s7ShowTransition ? t[lang].s7TransitionLabel : ''}</span>`, and one `.s7-anchor-pair` whose contents switch on `activeStepS7`:
      - When `activeStepS7 === 0`: two `.s7-anchor` items — first renders `kopiCount` figure + `{t[lang].s7KopiLabel}` label + citation line `{t[lang].s7SourcePrefix}Rp {fmtNum(constants?.anchors?.kopi?.price ?? 0)}` (also include the sourceLabel from constants); the second renders `seblakCount` + `{t[lang].s7SeblakLabel}` + matching citation. Figures in `var(--gold)`.
      - When `activeStepS7 === 1`: two `.s7-anchor` items — first renders `sdCount` + `{t[lang].s7SDLabel}` + citation; second renders `puskesmasCount` + `{t[lang].s7PuskesmasLabel}` + citation. Figures in `var(--red)`.
      - Numbers rendered via `{fmtCount(kopiCount, lang)}` etc. so locale grouping is correct.
      - Before `constants`/`stats` resolve, show `—` (em dash) instead of the count and apply `loading-pulse` class to the figure span (reuse existing `.loading-pulse` rule).
    Below the sticky panel content, include a `<div class="step-indicator" aria-hidden="true">` with two pips driven by `{#each [0,1] as s} <div class="pip" class:active={activeStepS7 === s}></div> {/each}` — matches the existing S5 `.step-indicator` pattern.
    The `.steps-col` contains two `<div class="step" data-step="N">` blocks with `.step-card`s. Step 0 card uses `s7Step0Heading`/`s7Step0Body` and includes two `<a class="source-link" href={constants?.anchors?.kopi?.source} target="_blank" rel="noopener noreferrer">{constants?.anchors?.kopi?.sourceLabel}</a>` lines (one for kopi, one for seblak). Step 1 card uses `s7Step1Heading`/`s7Step1Body` and includes two source-link lines (sd + puskesmas).

    **E. Scoped CSS** — Add inside the existing scoped `<style>` block (anywhere after the existing `.s6-transition-label` rules, before the `@media (max-width: 800px)` block):
      - `.s7-anchor-pair { display: flex; flex-direction: column; gap: var(--space-xl); margin-top: var(--space-xl); }`
      - `.s7-anchor { display: flex; flex-direction: column; gap: var(--space-xs); }`
      - `.s7-anchor-figure` — Libre Baskerville, weight 700, font-size `clamp(2.2rem, 5vw, 3.8rem)`, line-height 1.08, letter-spacing -0.02em. Color set via a modifier class — `.s7-anchor-figure.is-gold { color: var(--gold); }` and `.s7-anchor-figure.is-red { color: var(--red); }`. Apply the modifier inline via `class:is-gold={activeStepS7 === 0}` and `class:is-red={activeStepS7 === 1}` on the figure span.
      - `.s7-anchor-label { font-family: 'Source Serif 4', Georgia, serif; font-size: 0.9rem; color: var(--muted); line-height: 1.4; }`
      - `.s7-anchor-citation { font-family: 'JetBrains Mono', monospace; font-size: 0.7rem; color: var(--muted); line-height: 1.5; }`
      - `.s7-transition-label { display: block; min-height: 1.2em; font-family: 'JetBrains Mono', monospace; font-size: 0.7rem; font-style: italic; color: var(--muted); margin-bottom: var(--space-md); }`
      - `.s7-sticky-heading { font-family: 'Libre Baskerville', Georgia, serif; font-size: clamp(1.4rem, 3vw, 2rem); font-weight: 700; color: var(--text); margin: 0 0 var(--space-lg) 0; line-height: 1.2; }`

    Do NOT override S7 sticky behavior with the S1 `position: relative; height: auto` mobile rule — S7 is interactive (count-up animations) and must remain sticky on mobile. The existing `.scrolly .sticky-col` mobile rule at lines 754–768 applies correctly without override.

    Per UI-SPEC the s7TransitionLabel is reserved space (min-height) so its appearance/disappearance does not cause layout shift between Step 0 and Step 1.
  </action>
  <verify>
    <automated>cd /Users/yosef/Projects/idsterity/dashboard &amp;&amp; npm run build 2&gt;&amp;1 | tail -30 &amp;&amp; grep -v '^[[:space:]]*//' src/App.svelte | grep -c 'data-section="s7"' &amp;&amp; grep -v '^[[:space:]]*//' src/App.svelte | grep -c "makeScroller('s7'" &amp;&amp; grep -v '^[[:space:]]*//' src/App.svelte | grep -c 'countUp(' &amp;&amp; grep -v '^[[:space:]]*//' src/App.svelte | grep -c 'activeStepS7'</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with no Svelte compile errors and no warnings about undefined identifiers
    - `App.svelte` contains exactly one `data-section="s7"` attribute (excluding comments)
    - `App.svelte` contains `makeScroller('s7'` exactly once
    - `App.svelte` contains `countUp(` definition and at least 4 call sites (kopi, seblak, sd, puskesmas) — total ≥5 occurrences
    - `App.svelte` references `activeStepS7` in at least 5 locations (state decl, scroller callback, $effect, two HTML conditionals, optional pip indicator)
    - `App.svelte` references `constants.anchors.kopi.price`, `constants.anchors.seblak.price`, `constants.anchors.sd.price`, `constants.anchors.puskesmas.price` (or with optional chaining `constants?.anchors?.{name}?.price`)
    - `App.svelte` contains `<span class="s7-transition-label" aria-live="polite">` and binds its visible text to `s7ShowTransition`
    - Behavior on dev server: scrolling into S7 Step 0 starts kopiCount and seblakCount animations in gold; scrolling into Step 1 shows transition label, then sdCount + puskesmasCount animations in red ~300ms / ~450ms later
    - With `prefers-reduced-motion: reduce` set in DevTools rendering panel, counts snap to final values without RAF animation
    - Language toggle preserves scroll position and re-renders all S7 strings in the active language
  </acceptance_criteria>
  <done>S7 section renders on the dev server, fires four count-up animations driven by stats.labelPagu.high and constants.anchors.*, transitions between gold (Step 0) and red (Step 1), shows the aria-live transition label between beats, and respects prefers-reduced-motion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static JSON → browser | Pre-baked `constants.json` is committed; trusted source |
| user → DOM | No user-supplied input in S7; scroll position is the only user signal |
| browser → external sources | `<a href={anchor.source}>` opens external citation URLs in new tab |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | `constants.json` `anchors.*.price` | accept | Static file committed to repo; tampering requires PR. No client validation needed beyond `Math.floor(stats.labelPagu.high / price)` returning a sane integer (NaN guard via `?? 0` fallback). |
| T-03-02 | Information Disclosure | Source citation URLs | accept | URLs are public Indonesian news/government sources; no PII; expected behavior to disclose. |
| T-03-03 | Elevation of Privilege | `<a target="_blank">` reverse tabnabbing on source-link | mitigate | All `.source-link` anchors include `rel="noopener noreferrer"` per existing S2/S3 pattern at App.svelte lines 184, 195, 206. New S7 source-link anchors MUST replicate this rel attribute. |
| T-03-04 | Denial of Service (client) | RAF count-up loop running forever | mitigate | `countUp` loop terminates when `t >= 1`; clearTimeout on pending Step 1 staggers in onDestroy prevents zombie timers across HMR or navigation. |
| T-03-05 | Spoofing | XSS via unescaped i18n strings | mitigate | All copy rendered via Svelte `{t[lang].key}` interpolation which escapes HTML by default. No `{@html ...}` used anywhere in S7. |
</threat_model>

<verification>
1. `npm run build` exits 0 with no Svelte errors.
2. `python3 -m json.tool dashboard/public/data/constants.json` exits 0.
3. Dev server (`npm run dev`) loads at `/`, hero renders, scroll past S6 reveals the S7 section.
4. S7 Step 0: kopi + seblak count-up to final values in gold within ~2s.
5. S7 Step 1: transition label appears (Indonesian on default load), SD count-up starts ~300ms later, puskesmas ~450ms later, both red.
6. Scroll back to S6 and forward to S7 again — counts re-animate from 0.
7. Toggle language → all S7 copy switches to English, scroll position preserved.
8. DevTools rendering → emulate `prefers-reduced-motion: reduce` → counts snap immediately.
9. 375px viewport: S7 stacks vertically, sticky-col is `50dvh` at top, count figures still readable.
</verification>

<success_criteria>
- SEC-07 requirement fully delivered: total "high" spending displayed as 4 anchored count-up animations in both languages with cited unit prices.
- Phase 3 Success Criterion #1 from ROADMAP satisfied: "S7 animates the total 'absurd' spending as count-up reveals, one anchor at a time (kopi jago cups, seblak portions, elementary schools, puskesmas) — both languages, with cited unit prices."
- Vertical slice complete: data layer (constants.anchors) + copy layer (i18n S7 keys) + UI layer (App.svelte section, state, scrollama, RAF utility, CSS) all wired end-to-end in one plan.
</success_criteria>

<output>
After completion, create `.planning/phases/03-interactive-back-half-s7-s9/03-01-SUMMARY.md` summarizing:
- New constants.anchors structure (4 entries, source citations)
- 12 new i18n keys (id + en)
- App.svelte additions: 7 $state vars, countUp utility, makeScroller('s7'), $effect, S7 section HTML, scoped CSS
- Confirmed verification steps (build pass, dev-server behavior, reduced-motion behavior)
</output>
</content>
</invoke>