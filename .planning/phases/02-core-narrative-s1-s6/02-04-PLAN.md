---
phase: 02-core-narrative-s1-s6
plan: 04
type: execute
wave: 3
depends_on: [02-01, 02-02, 02-03]
files_modified:
  - dashboard/src/App.svelte
autonomous: false
requirements: [SEC-04, MOB-01, MOB-03]
must_haves:
  truths:
    - "S4 renders as a full-width non-sticky non-scroll block immediately after S3"
    - "S4 shows total procurement pagu as a hero-size display number in --gold"
    - "S4 shows total record count as a hero-size display number in --gold"
    - "S4 shows three label-breakdown rows (high/med/low) each with colored dot, label, count, and pagu amount"
    - "S4 shows a small JetBrains Mono safe-harbour disclaimer line at the bottom with italic styling and --muted color"
    - "On 375px viewport the stats grid collapses from 2-column to 1-column"
    - "S4 reads totals from the stats $state populated by safeFetch in onMount"
  artifacts:
    - path: "dashboard/src/App.svelte"
      provides: "S4 Dataset Overview full-width block markup + CSS"
      contains: "data-section=\"s4\""
  key_links:
    - from: "App.svelte stats state"
      to: "S4 totalPagu and totalRecords renderers"
      via: "fmtT helper and toLocaleString in template"
      pattern: "stats\\.totalPagu"
    - from: "App.svelte stats.labelPagu and stats.labelCounts"
      to: "S4 label breakdown rows"
      via: "stats.labelPagu.high, stats.labelCounts.high (similarly med, low)"
      pattern: "labelPagu\\.high"
---

## Phase Goal

**As a** visitor, **I want to** see the dataset's overall scale and AI-label breakdown after the macro context, **so that I** grasp how the millions of procurement records map to wajar / perlu dicermati / bermasalah categories before the narrative drills into institutions.

<objective>
Add the S4 Dataset Overview block to App.svelte. Per D-01, S4 has no sticky panel and no scroll steps - it is a full-width centered stats block sitting between S3 (GDP chart) and the S5+S6 institutions section (built in Plan 05). The block consumes `stats` (already fetched in Plan 01 via safeFetch) and shows: total pagu (Rp X T) and total record count as two hero-size numbers, three label-breakdown rows (high, med, low - color-encoded), and a safe-harbour disclaimer (D-07).

Purpose: Connect the macro irony (S2/S3) to the procurement specifics that follow (S5/S6). Without S4 the visitor jumps from GDP percentages to institution rankings with no sense of dataset scale.
Output: Updates to App.svelte adding the S4 markup block and accompanying scoped styles. No new files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-narrative-s1-s6/02-CONTEXT.md
@.planning/phases/02-core-narrative-s1-s6/02-UI-SPEC.md
@.planning/phases/02-core-narrative-s1-s6/02-PATTERNS.md
@.planning/phases/02-core-narrative-s1-s6/02-01-PLAN.md
@.planning/phases/02-core-narrative-s1-s6/02-03-PLAN.md
@dashboard/src/App.svelte
@dashboard/public/data/summary-stats.json

<interfaces>
After Plans 01-03 the App.svelte stats state object (populated by safeFetch from /data/summary-stats.json) has this shape:
- stats.totalPagu (integer IDR)
- stats.totalRecords (integer)
- stats.flaggedCount (integer)
- stats.flaggedPagu (integer)
- stats.uniqueLembaga (integer)
- stats.labelPagu = { low, med, high, unflagged } (all integers IDR; unflagged guaranteed >=0 after Plan 02's CR-03 fix)
- stats.labelCounts = { low, med, high, unflagged } (all integers; unflagged guaranteed >=0)

App.svelte already exposes the helpers fmtT(v) and fmtNum(v) for formatting trillions and locale numbers; reuse them.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add S4 markup block + scoped styles to App.svelte</name>
  <files>dashboard/src/App.svelte</files>
  <read_first>
    - dashboard/src/App.svelte (current state after Plans 01 and 03 - script has fmtT and fmtNum helpers; template has hero, S1, S2, S3 sections in order; styles include hero-stat / hero-number / loading-pulse patterns to reuse)
    - .planning/phases/02-core-narrative-s1-s6/02-UI-SPEC.md section "S4 - Dataset Overview (no sticky, no scroll)" (full-width block; background var(--bg-alt); padding var(--space-page) top and bottom; max-width 800px centered; eyebrow > heading > 2-row stats grid > AI-label breakdown > disclaimer; stat numbers in display size --gold via .hero-number style reuse; color dots for the three labels: --red high, --amber med, rgba(237,232,220,0.3) low; disclaimer JetBrains Mono 11px italic max-width 520px border-top 1px var(--border))
    - .planning/phases/02-core-narrative-s1-s6/02-UI-SPEC.md section "Copywriting Contract > S4" (i18n keys: s4Eyebrow, s4Heading, s4TotalPaguLabel, s4RecordCountLabel, s4LabelBreakdownHeading, s4LabelLow, s4LabelMed, s4LabelHigh, s4Disclaimer - all added to i18n.js in Plan 01)
    - .planning/phases/02-core-narrative-s1-s6/02-PATTERNS.md section "Loading/error state pattern" (use loading-pulse class while stats is null)
    - dashboard/public/data/summary-stats.json (real data shape - confirm fields present after Plan 02 regen)
  </read_first>
  <action>
    Modify dashboard/src/App.svelte to add the S4 block.

    Template addition: insert immediately after the closing </section> of the S3 scrolly section (from Plan 03) and before the {#if fetchError} block:

    A <section class="s4" data-section="s4" id="s4"> wrapper containing an inner <div class="s4-inner"> with these children in order:

    1. <div class="eyebrow">{t[lang].s4Eyebrow}</div>
    2. <h2 class="s4-heading">{t[lang].s4Heading}</h2>
    3. A <div class="s4-stats-grid"> containing two .s4-stat-cell children. Each cell renders {#if stats} <div class="s4-stat-number">...</div> <div class="s4-stat-label">...</div> {:else} <div class="s4-stat-number loading-pulse">--</div> <div class="s4-stat-label">{t[lang].loading}</div> {/if}. Cell 1 number: `Rp {fmtT(stats.totalPagu)} T`, label: {t[lang].s4TotalPaguLabel}. Cell 2 number: `{fmtNum(stats.totalRecords)}`, label: {t[lang].s4RecordCountLabel}.
    4. <h3 class="s4-breakdown-heading">{t[lang].s4LabelBreakdownHeading}</h3>
    5. A <ul class="s4-breakdown"> with three <li class="s4-row"> children, in order high, med, low (visual descending severity). Each row contains: a <span class="s4-dot" style="background: ..."></span> with inline style background var(--red) for high, var(--amber) for med, rgba(237,232,220,0.3) for low; a <span class="s4-row-label">{t[lang].s4LabelHigh}</span> (or med, low); a <span class="s4-row-count">{stats ? fmtNum(stats.labelCounts.high) : '--'} paket</span> (translate 'paket' via lang: id='paket', en='packages' - inline ternary since copy is identical for all three rows); a <span class="s4-row-pagu">Rp {stats ? fmtT(stats.labelPagu.high) : '--'} T</span>. Apply for med and low rows similarly.
    6. <p class="s4-disclaimer">{t[lang].s4Disclaimer}</p>

    Style additions (under a new /* -- S4 Dataset -- */ comment divider inside the existing <style> block):

    .s4 { background: var(--bg-alt); padding: var(--space-page) var(--space-xl); border-bottom: 1px solid var(--border); }
    .s4-inner { max-width: 800px; margin: 0 auto; }
    .s4-heading { font-family: 'Libre Baskerville', Georgia, serif; font-size: clamp(1.8rem, 4vw, 2.6rem); font-weight: 700; line-height: 1.2; color: var(--text); margin: 0 0 var(--space-2xl) 0; letter-spacing: -0.01em; }
    .s4-stats-grid { display: grid; grid-template-columns: 1fr 1fr; gap: var(--space-xl); margin-bottom: var(--space-3xl); }
    .s4-stat-cell { padding: var(--space-lg); border: 1px solid var(--border); border-radius: 3px; background: rgba(255,255,255,0.02); }
    .s4-stat-number { font-family: 'Libre Baskerville', Georgia, serif; font-size: clamp(2.2rem, 5vw, 3.8rem); font-weight: 700; color: var(--gold); line-height: 1; margin-bottom: var(--space-sm); letter-spacing: -0.02em; }
    .s4-stat-label { font-size: 0.88rem; color: var(--muted); font-style: italic; line-height: 1.5; }
    .s4-breakdown-heading { font-family: 'Libre Baskerville', Georgia, serif; font-size: 1.45rem; font-weight: 700; color: var(--text); margin: 0 0 var(--space-md) 0; }
    .s4-breakdown { list-style: none; padding: 0; margin: 0 0 var(--space-lg) 0; }
    .s4-row { display: flex; align-items: center; gap: var(--space-md); padding: var(--space-sm) 0; border-bottom: 1px solid var(--border); }
    .s4-row:last-child { border-bottom: none; }
    .s4-dot { display: inline-block; width: 10px; height: 10px; border-radius: 50%; flex-shrink: 0; }
    .s4-row-label { flex: 1; color: var(--text); font-size: 1rem; }
    .s4-row-count, .s4-row-pagu { font-family: 'JetBrains Mono', monospace; font-size: 0.8rem; color: var(--muted); }
    .s4-disclaimer { font-family: 'JetBrains Mono', monospace; font-size: 0.7rem; color: var(--muted); font-style: italic; max-width: 520px; margin: var(--space-md) 0 0 0; padding-top: var(--space-md); border-top: 1px solid var(--border); line-height: 1.6; }

    Add to the existing @media (max-width: 800px) block at the bottom of the style sheet: .s4-stats-grid { grid-template-columns: 1fr; } and .s4 { padding: var(--space-2xl) var(--space-lg); } (override the page-size padding on narrower screens).

    Do NOT modify any pre-existing sections (hero, S1, S2, S3 scrolly blocks). Do NOT touch the fetchError block.
  </action>
  <verify>
    <automated>grep -c 'data-section="s4"' /Users/yosef/Projects/idsterity/dashboard/src/App.svelte &amp;&amp; grep -c 's4-stats-grid' /Users/yosef/Projects/idsterity/dashboard/src/App.svelte &amp;&amp; grep -c 's4-disclaimer' /Users/yosef/Projects/idsterity/dashboard/src/App.svelte &amp;&amp; grep -c 'labelPagu' /Users/yosef/Projects/idsterity/dashboard/src/App.svelte &amp;&amp; grep -c 'labelCounts' /Users/yosef/Projects/idsterity/dashboard/src/App.svelte &amp;&amp; grep -c 's4LabelHigh' /Users/yosef/Projects/idsterity/dashboard/src/App.svelte &amp;&amp; cd /Users/yosef/Projects/idsterity/dashboard &amp;&amp; npm run build 2&gt;&amp;1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - App.svelte template contains one `<section class="s4" data-section="s4" id="s4">` block
    - The section's children include in order: eyebrow div, s4-heading h2, s4-stats-grid div with exactly 2 cells, s4-breakdown-heading h3, s4-breakdown ul with exactly 3 li rows, s4-disclaimer p
    - Each li row has classes s4-dot, s4-row-label, s4-row-count, s4-row-pagu
    - The first row in markup order references labelPagu.high and labelCounts.high
    - .s4-stat-number CSS rule uses color var(--gold)
    - The mobile breakpoint block overrides .s4-stats-grid to grid-template-columns: 1fr
    - npm run build exits 0
  </acceptance_criteria>
  <done>S4 block renders between S3 and the (Plan 05) institutions section, color-encoded breakdown rows are correct, mobile breakpoint collapses to single column, disclaimer renders.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification - S4 stats block on desktop and 375px</name>
  <what-built>
    The dataset overview block. Visitor scrolls past S3 GDP chart and arrives at a full-width centered stats panel showing total procurement pagu (Rp 642 T) and total record count (3.0 million) as hero-size gold numbers, followed by an AI-label breakdown listing high / med / low counts and pagu amounts each with a colored dot, finished by a safe-harbour disclaimer in muted JetBrains Mono italic.
  </what-built>
  <read_first>
    - dashboard/src/App.svelte (post-Task-1 state)
    - dashboard/public/data/summary-stats.json (so the developer knows what numbers should appear)
  </read_first>
  <how-to-verify>
    1. Run `cd dashboard && npm run dev` and open the URL.
    2. Scroll past hero, S1, S2, S3. Confirm S4 enters as a full-width centered block on var(--bg-alt) background (slightly lighter than the page background).
    3. Confirm the eyebrow "S4 - DATASET 2026" appears in gold.
    4. Confirm the heading "Apa yang ada di dataset ini?" (id) or "What's in this dataset?" (en) renders in Libre Baskerville.
    5. Confirm two stat cells side by side (desktop): left cell shows "Rp 642.2 T" or similar in gold + label "Total pagu pengadaan"; right cell shows "3,009,760" + label "Paket pengadaan".
    6. Confirm three breakdown rows below: high row has a RED dot + label "Bermasalah" + count "24,998 paket" + pagu "Rp 10.7 T"; med row has an AMBER dot + label + count + pagu; low row has a muted dot + label "Wajar" + count + pagu.
    7. Confirm the disclaimer at the bottom renders in JetBrains Mono italic at small size: "Label dihasilkan oleh model AI. Hanya untuk referensi. Bukan penilaian hukum."
    8. Toggle language. All eight strings switch to English. Stat numbers remain unchanged.
    9. Resize to 375px. Confirm the two stat cells collapse to a single column (one cell above the other), the padding reduces, no horizontal overflow, the disclaimer wraps within the viewport.
    10. (Sanity) Open DevTools and confirm there are no console warnings about missing stats fields when navigating with the slow-3G throttle (catches a race condition if the block tries to render numbers before stats arrives).
  </how-to-verify>
  <acceptance_criteria>
    - All 10 verification steps confirmed
    - Color encoding for breakdown rows correct: high=red dot, med=amber dot, low=muted dot
    - No horizontal overflow at 375px
    - Loading pulse renders before stats arrives (visible briefly on slow throttle)
  </acceptance_criteria>
    <files>(human verification only — no files modified)</files>
  <action>This is a human-verification checkpoint. Claude pauses execution here. The developer performs the verification steps listed in &lt;how-to-verify&gt; above and reports the result via the resume-signal. No file modifications are performed by Claude in this task.</action>
  <verify>
    <automated>echo "Human verification checkpoint — no automated verification. Resume signal acceptance is required from the developer."</automated>
  </verify>
  <done>Developer has responded "approved" to the resume-signal, OR provided a list of failing verification steps which the executor must address in a follow-up task before re-attempting this checkpoint.</done>
  <resume-signal>Type "approved" or describe the failing step.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stats state -> S4 template | Numeric integer values; the template uses fmtT and toLocaleString helpers; no user input is interpolated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02.04-01 | Tampering | summary-stats.json labelPagu/labelCounts unflagged values | mitigate | Already addressed in Plan 02 (CR-03 fix) - unflagged is max(0, ...) guaranteed non-negative so the S4 breakdown rows cannot show absurd "Rp -X T" values |
| T-02.04-02 | Information disclosure | S4 stats numbers | accept | All values are aggregated from public LPSE/SIRUP records; broadcasting them is the project goal |
| T-02.04-03 | Denial of service | S4 render path | accept | S4 is static markup with three label rows; no loops over user-controlled lengths |
</threat_model>

<verification>
- npm run build exits 0
- S4 section renders in DOM between S3 and the (still empty) S5+S6 placement
- Visual verification on desktop and 375px
- Loading pulse visible before stats arrives
</verification>

<success_criteria>
- SEC-04 demonstrably satisfied: visitor sees total pagu, total records, three-way AI label breakdown, and safe-harbour disclaimer in both languages
- MOB-01 / MOB-03 honored: 375px viewport renders the block without overflow; stats grid collapses; disclaimer wraps
- No external service or new file is introduced - this plan is purely a markup/style additive
</success_criteria>

<output>
After completion, create .planning/phases/02-core-narrative-s1-s6/02-04-SUMMARY.md summarising the S4 block layout, the actual stat numbers rendered (so reviewers can sanity-check against summary-stats.json), and any deviations from the UI-SPEC.
</output>
